{
  "release": "v150",
  "featureSlug": "media-element-pseudo-classes",
  "chromestatusId": 5068277495758848,
  "specUrl": "https://html.spec.whatwg.org/multipage/semantics-other.html#pseudo-classes",
  "generatedAt": "2026-06-01T08:14:34Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "playing-pseudo-class",
      "description": "The :playing pseudo-class parses as a valid selector.",
      "kind": "script",
      "test": "(()=>{try{return document.createElement('video').matches(':playing')||true;}catch(_){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/semantics-other.html#pseudo-classes"
    },
    {
      "id": "paused-pseudo-class",
      "description": ":paused parses and matches a default (paused-by-default) <video>.",
      "kind": "script",
      "test": "(()=>{const v=document.createElement('video');try{return v.matches(':paused');}catch(_){return false;}})()"
    },
    {
      "id": "seeking-pseudo-class",
      "description": ":seeking parses on a <video> element without throwing.",
      "kind": "script",
      "test": "(()=>{try{document.createElement('video').matches(':seeking');return true;}catch(_){return false;}})()"
    },
    {
      "id": "muted-pseudo-class",
      "description": ":muted parses and matches a <video> with muted=true.",
      "kind": "script",
      "test": "(()=>{const v=document.createElement('video');v.muted=true;try{return v.matches(':muted');}catch(_){return false;}})()"
    },
    {
      "id": "volume-locked-pseudo-class",
      "description": ":volume-locked parses without throwing.",
      "kind": "script",
      "test": "(()=>{try{document.createElement('audio').matches(':volume-locked');return true;}catch(_){return false;}})()"
    },
    {
      "id": "buffering-pseudo-class",
      "description": ":buffering and :stalled parse together as a selector list.",
      "kind": "script",
      "test": "(()=>{try{document.createElement('video').matches(':buffering, :stalled');return true;}catch(_){return false;}})()"
    },
    {
      "id": "stylesheet-with-media-pseudo",
      "description": "Stylesheet rules using media pseudo-classes parse without being dropped.",
      "kind": "script",
      "test": "(()=>{const s=document.createElement('style');s.textContent='video:playing{outline:1px solid red}video:paused{outline:1px solid green}';document.head.appendChild(s);const ok=s.sheet.cssRules.length===2;s.remove();return ok;})()"
    }
  ]
}
