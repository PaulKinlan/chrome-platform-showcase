{
  "release": "v150",
  "featureSlug": "css-image-color-function",
  "chromestatusId": 5121011285622784,
  "specUrl": "https://drafts.csswg.org/css-images-4/#image-notation",
  "generatedAt": "2026-06-01T08:14:34Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "image-color-as-background",
      "description": "background-image: image(<color>) parses via CSS.supports.",
      "kind": "css-supports",
      "test": "background-image: image(red)",
      "specSection": "https://drafts.csswg.org/css-images-4/#image-notation"
    },
    {
      "id": "image-color-rgba",
      "description": "image() accepts <color> in functional notation including alpha.",
      "kind": "css-supports",
      "test": "background-image: image(rgb(10 20 30 / 0.5))"
    },
    {
      "id": "image-color-named",
      "description": "image() accepts named colors.",
      "kind": "css-supports",
      "test": "background-image: image(rebeccapurple)"
    },
    {
      "id": "image-color-list-style",
      "description": "image(<color>) is accepted anywhere <image> is — including list-style-image.",
      "kind": "css-supports",
      "test": "list-style-image: image(blue)"
    },
    {
      "id": "image-color-cssom-roundtrip",
      "description": "background-image: image(red) round-trips via getComputedStyle (computed value contains a colour or url representation, not the literal 'image(red)' string left unparsed).",
      "kind": "script",
      "test": "(()=>{const e=document.createElement('div');e.style.backgroundImage='image(red)';document.body.appendChild(e);const v=getComputedStyle(e).backgroundImage;e.remove();return v!=='' && v!=='none';})()"
    },
    {
      "id": "image-rejects-non-color-arg",
      "description": "image() with a single bare keyword that is neither a colour nor a URL is rejected (regression check on the grammar).",
      "kind": "script",
      "test": "!CSS.supports('background-image','image(not-a-thing-keyword)')"
    }
  ]
}
