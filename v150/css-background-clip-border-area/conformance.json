{
  "release": "v150",
  "featureSlug": "css-background-clip-border-area",
  "chromestatusId": 6234471210811392,
  "specUrl": "https://drafts.csswg.org/css-backgrounds-4/#valdef-background-clip-border-area",
  "generatedAt": "2026-06-01T08:14:34Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "background-clip-property-baseline",
      "description": "background-clip exists and accepts the legacy border-box keyword (regression baseline).",
      "kind": "css-supports",
      "test": "background-clip: border-box",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "background-clip-border-area-supported",
      "description": "The new border-area keyword parses via CSS.supports.",
      "kind": "css-supports",
      "test": "background-clip: border-area",
      "specSection": "https://drafts.csswg.org/css-backgrounds-4/#valdef-background-clip-border-area"
    },
    {
      "id": "background-clip-text-still-supported",
      "description": "The text keyword still parses; border-area is additive, not a replacement.",
      "kind": "css-supports",
      "test": "background-clip: text"
    },
    {
      "id": "border-area-computed-roundtrip",
      "description": "Setting background-clip: border-area via CSSOM round-trips through getComputedStyle.",
      "kind": "script",
      "test": "(()=>{const e=document.createElement('div');e.style.backgroundClip='border-area';document.body.appendChild(e);const v=getComputedStyle(e).backgroundClip;e.remove();return /border-area/i.test(v);})()"
    },
    {
      "id": "background-clip-invalid-keyword-rejected",
      "description": "Bogus keywords are still rejected (regression check that the parser remains strict).",
      "kind": "script",
      "test": "!CSS.supports('background-clip','this-is-not-a-real-keyword')"
    },
    {
      "id": "border-area-respects-border-width",
      "description": "An element with a thick border, background-color, and background-clip: border-area exposes a non-zero offsetWidth — the demo is observable in the DOM.",
      "kind": "script",
      "test": "(()=>{const e=document.createElement('div');e.style.cssText='width:80px;height:80px;border:10px dashed black;background:red;background-clip:border-area';document.body.appendChild(e);const ok=e.offsetWidth===100;e.remove();return ok;})()"
    }
  ]
}
