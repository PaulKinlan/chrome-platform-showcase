{
  "release": "v150",
  "featureSlug": "email-verification-protocol",
  "chromestatusId": 5205725253074944,
  "specUrl": "https://dickhardt.github.io/email-verification/draft-hardt-email-verification.html",
  "generatedAt": "2026-06-01T08:14:34Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "credentials-api-exists",
      "description": "navigator.credentials exists — EVP plugs into the Credentials Management surface.",
      "kind": "exists",
      "test": "navigator.credentials",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "credentials-get-is-function",
      "description": "navigator.credentials.get is a function — the dispatch point for federated/email credential requests.",
      "kind": "typeof",
      "test": "navigator.credentials.get",
      "expect": "function",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "email-credential-options-shape",
      "description": "Calling navigator.credentials.get({email:{...}}) is constructible (no throw at parse time even if not supported).",
      "kind": "script",
      "test": "(()=>{try{const p=navigator.credentials.get({email:{verifier:'https://example.com/evp'},mediation:'optional'});if(p&&typeof p.then==='function'){p.catch(()=>{});}return true;}catch(_){return false;}})()"
    },
    {
      "id": "emailcredential-interface-exposed",
      "description": "EmailCredential interface is exposed (the per-credential type used by EVP).",
      "kind": "exists",
      "test": "EmailCredential"
    },
    {
      "id": "secure-context-required",
      "description": "window.isSecureContext is true for any host that wants to use EVP; the API is gated to secure contexts.",
      "kind": "script",
      "test": "typeof isSecureContext==='boolean'",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    }
  ]
}
