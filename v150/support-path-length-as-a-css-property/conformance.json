{
  "release": "v150",
  "featureSlug": "support-path-length-as-a-css-property",
  "chromestatusId": 4861677550043136,
  "specUrl": "https://svgwg.org/svg2-draft/paths.html#PathLengthAttribute",
  "generatedAt": "2026-07-18T17:30:00Z",
  "author": "weekly-hardening-routine",
  "assertions": [
    {
      "id": "path-length-property-supported",
      "description": "path-length is accepted as a CSS property that mirrors the SVG pathLength attribute.",
      "kind": "css-supports",
      "test": "path-length: 1000",
      "specSection": "https://drafts.fxtf.org/motion-1/"
    },
    {
      "id": "path-length-accepts-zero",
      "description": "path-length accepts 0 (the degenerate edge case).",
      "kind": "css-supports",
      "test": "path-length: 0"
    },
    {
      "id": "path-length-computed-roundtrip",
      "description": "Setting path-length via CSSOM round-trips through getComputedStyle on an SVG path.",
      "kind": "script",
      "test": "(()=>{if(!CSS.supports('path-length','1000'))return false;const p=document.createElementNS('http://www.w3.org/2000/svg','path');p.style.setProperty('path-length','1000');return p.style.getPropertyValue('path-length').trim()==='1000';})()"
    }
  ]
}
