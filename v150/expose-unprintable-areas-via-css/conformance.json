{
  "release": "v150",
  "featureSlug": "expose-unprintable-areas-via-css",
  "chromestatusId": 5515971464527872,
  "specUrl": "https://drafts.csswg.org/css-page-3/#page-margin-safety",
  "generatedAt": "2026-06-01T08:14:34Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "at-page-baseline",
      "description": "@page parses (regression baseline) — @page { } is a known at-rule.",
      "kind": "script",
      "test": "(()=>{const s=document.createElement('style');s.textContent='@page{margin:1cm}';document.head.appendChild(s);const ok=s.sheet.cssRules.length===1;s.remove();return ok;})()",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "env-safe-printable-inset",
      "description": "env(safe-printable-inset) parses as a CSS length with a fallback value.",
      "kind": "css-supports",
      "test": "margin-top: env(safe-printable-inset, 0)",
      "specSection": "https://drafts.csswg.org/css-env-1/#safe-printable-inset"
    },
    {
      "id": "env-safe-printable-inset-bottom",
      "description": "env(safe-printable-inset) can be used for bottom margins with a fallback value.",
      "kind": "css-supports",
      "test": "margin-bottom: env(safe-printable-inset, 0)"
    },
    {
      "id": "env-safe-printable-inset-inline",
      "description": "env(safe-printable-inset) can be used for inline margins with a fallback value.",
      "kind": "css-supports",
      "test": "margin-left: env(safe-printable-inset, 0)"
    },
    {
      "id": "env-fallback-applied-on-screen",
      "description": "On screen media, env(safe-printable-inset, 25px) resolves to its fallback value because the printer-provided inset is print-context dependent.",
      "kind": "script",
      "test": "(()=>{const e=document.createElement('div');e.style.cssText='margin-top:env(safe-printable-inset, 25px)';document.body.appendChild(e);const v=getComputedStyle(e).marginTop;e.remove();return v==='25px' || parseFloat(v)>0;})()"
    },
    {
      "id": "max-of-margin-and-printer-min",
      "description": "max(env(safe-printable-inset, 0), 1cm) parses — the canonical protective usage of the variable.",
      "kind": "css-supports",
      "test": "margin-top: max(env(safe-printable-inset, 0px), 1cm)"
    }
  ]
}
