.DS_Store
node_modules/
.env
*.log
.deno/
.pi-subagents/

# Responsive harness screenshots (regenerable evidence; keep JSON run reports, not PNGs)
reports/responsive/**/*.png

# Scheduled work-list outputs are uploaded as workflow artifacts.
reports/demo-worklist.json
reports/demo-worklist.md

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
