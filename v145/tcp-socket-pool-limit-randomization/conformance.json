{
  "release": "v145",
  "featureSlug": "tcp-socket-pool-limit-randomization",
  "chromestatusId": 6496757559197696,
  "specUrl": "https://fetch.spec.whatwg.org/#http-network-fetch",
  "generatedAt": "2026-06-01T08:14:32Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "fetch-still-exists",
      "description": "fetch is exposed — the surface used for any cross-site fetch the new randomization indirectly affects.",
      "kind": "typeof",
      "test": "fetch",
      "expect": "function",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "performance-resource-timing-exists",
      "description": "PerformanceResourceTiming is exposed — the timing surface that would expose any observable jitter from connection pool randomization.",
      "kind": "exists",
      "test": "PerformanceResourceTiming",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "resource-timing-connect-fields",
      "description": "PerformanceResourceTiming.prototype exposes connectStart/connectEnd — the timings most relevant to socket pool behaviour.",
      "kind": "script",
      "test": "(()=>{try{return 'connectStart' in PerformanceResourceTiming.prototype&&'connectEnd' in PerformanceResourceTiming.prototype;}catch(e){return false;}})()",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "xhr-still-exists",
      "description": "XMLHttpRequest is exposed — the legacy surface that also routes through the affected socket pools.",
      "kind": "exists",
      "test": "XMLHttpRequest",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "websocket-still-exists",
      "description": "WebSocket is exposed — another consumer of HTTP connection budgets that the randomization governs.",
      "kind": "exists",
      "test": "WebSocket",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    }
  ]
}
