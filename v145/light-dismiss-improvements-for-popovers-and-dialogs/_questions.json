{
  "release": "v145",
  "featureSlug": "light-dismiss-improvements-for-popovers-and-dialogs",
  "chromestatusId": 6209615938322432,
  "reviewedAt": "2026-08-28T00:00:00Z",
  "reviewer": "claude-opus-5-v145-build (feature-level summary)",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "The closedby matrix is measured on real dialogs in all three states rather than described, which is how the modality-dependent missing-value default (none for show(), closerequest for showModal()) is shown rather than asserted. The dismissal mechanism is presented as an event-ordering question, which is what the HTML change actually is."
    },
    "interactive": {
      "state": "pass",
      "rationale": "A live matrix that opens and closes real modal dialogs while you wait, plus three openable dialogs to try Escape and backdrop clicks against; an event recorder that marks the moment of closure inside the sequence; and a menu-over-a-list checklist that detects each gesture and judges it from the menu's state rather than from self-report."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "The index frames the change as choosing which event means 'click' — pointerdown versus click — and names the two bugs that follow from the earlier choice, which is the pull request's own framing."
    },
    "edge_cases": {
      "state": "pass",
      "rationale": "Invalid and empty closedby values, case-insensitive keyword matching, the popover attribute's opposite invalid-value default, a dialog whose only exit is its own button, and a menu item activation that closes the popover for a reason that is neither light dismiss nor a close request."
    },
    "references": {
      "state": "pass",
      "rationale": "chromestatus.com/feature/6209615938322432 on the index and all three concept pages, with WHATWG HTML PR 11536, the closedby attribute section and MDN's Popover API."
    },
    "conformance": {
      "state": "pass",
      "rationale": "Eight immutable assertions: popover and closedby exist; keyword matching is case-insensitive; the missing-value default differs by modality; an invalid closedby never resolves to 'any'; dispatched input does not light-dismiss; the popover attribute's invalid-value default is manual; and CloseWatcher backs the close request."
    },
    "runtime_evidence": {
      "state": "partial",
      "rationale": "deno task drive over all three pages in real Chrome 150: 3/3, 27 assertions. Fully verified: the closedby matrix in all three states, the case-insensitive and invalid-value behaviour, the popover comparison, a modal dialog opening and closing with its returnValue, the recorder's sequence and labelling, and that a synthetic pointerdown/pointerup/click sequence leaves an open popover open. NOT verified: that a real click outside dismisses, that a real touch drag does not, and that a real right-click does not — those need trusted input, which this harness cannot produce, and the pages say so where a reader will see it rather than only here."
    },
    "telemetry_health": {
      "state": "pass",
      "rationale": "Each page emits showcaseTelemetry.assert with the measured defaults, the event preceding the close, whether synthetic input was ignored, and a per-gesture pass or fail."
    },
    "design_system": {
      "state": "pass",
      "rationale": "CSS variables throughout, buttons set background and colour together including the transparent menu items, 44px minimums on the page controls with a deliberate 36px inside the compact menu and toolbar, and the wide tables in their own scroll containers. The dialog backdrop is the one place a raw colour appears, as an alpha black that has no token."
    },
    "accessibility": {
      "state": "pass",
      "rationale": "Every dialog is labelled by its heading; the popover menu carries an aria-label; results are announced through aria-live; the checklist verdicts are sentences rather than colour alone; and each gesture card names what should happen before you try it, so the expectation is readable without doing it."
    },
    "mobile_desktop_parity": {
      "state": "pass",
      "rationale": "The toolbar and button rows wrap; the list scroller uses overscroll-behavior:contain so a drag inside it does not chain to the page; the recorder table scrolls in its own container; the dialogs are max-width constrained. Mobile is the class where the scroll gesture matters most, and the demo is built for a touch drag rather than a mouse wheel."
    }
  },
  "openQuestions": [
    {
      "title": "Drive real pointer input through CDP and complete the checklist automatically",
      "detail": "Input.dispatchTouchEvent and Input.dispatchMouseEvent produce trusted input, which is exactly what light dismiss requires. With those, the survives-the-gesture checklist could be completed by the harness — a touch drag, a right-click and a click — and the recorder could assert that the close lands on the click rather than the pointerdown. That is the single change that would move this feature's runtime evidence from partial to pass, and it is the same missing primitive the scroll-axis-lock feature needs.",
      "severity": "major"
    },
    {
      "title": "Compare against a browser with the old behaviour",
      "detail": "Chrome 150 already has the corrected behaviour, so the recorder can only ever show the good sequence here. A browser predating the change would show the close landing on pointerdown, and the page is written to say so if it ever sees it — but that branch has never run. Worth a note in the demo that the reading is one-sided on any current Chrome.",
      "severity": "minor"
    }
  ],
  "guidanceConsulted": [
    {
      "id": "light-dismiss-a-dialog",
      "query": "accessible popover and dialog light dismiss",
      "recommendation": "Use closedby on a dialog to opt into light dismiss explicitly rather than reimplementing outside-click handling, and let the platform own focus and dismissal.",
      "appliedOrException": "The three demonstration dialogs use closedby=\"any\", \"closerequest\" and \"none\" with no dismissal code of any kind; the menu is a plain auto popover opened with popovertarget. Nothing on these pages implements outside-click handling, which is what makes the recordings measurements of the browser rather than of the page.",
      "evidence": "The matrix reads closedBy back from real dialogs; the recorder's close entries come from beforetoggle, not from a handler that closed anything."
    },
    {
      "id": "declarative-dialog-popover-control",
      "query": "toggle a popover from a button without javascript",
      "recommendation": "Open overlays with popovertarget rather than script so the platform manages the top layer, focus and dismissal.",
      "appliedOrException": "Both popovers are opened by popovertarget buttons. The only scripted show/hide is in the synthetic-input control, and it now marks its own close so the recorder does not attribute it to the preceding event — a bug this build shipped briefly and caught in its own drive output.",
      "evidence": "The recorder now prints '▸ POPOVER CLOSED (by this page)' and refuses to interpret the sequence around it."
    },
    {
      "id": "accessibility",
      "query": "announce the result of a gesture the user performs",
      "recommendation": "Announce outcomes in text, and do not require the user to interpret a colour or to self-report what happened.",
      "appliedOrException": "Each gesture card's verdict is a sentence written from a measurement taken 160ms after the gesture; the summary counts progress in a live region. The user performs the gesture; the page decides what it means.",
      "evidence": "deno task drive scrolls the list programmatically and asserts the scroll card's verdict text changed from its untried state."
    }
  ],
  "summary": "Three concepts about an event-ordering change. Everything reachable without trusted input is measured — the closedby matrix including the modality-dependent default, the invalid-value behaviour, and the fact that dispatched input is correctly ignored — and the three gestures that need a real pointer are handed to the reader with the page doing the judging. The build's own scripted cleanup briefly made the recorder claim a click had dismissed a popover it had closed itself; the recorder now labels a scripted close and declines to interpret it."
}
