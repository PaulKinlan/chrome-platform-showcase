{
  "release": "v145",
  "featureSlug": "light-dismiss-improvements-for-popovers-and-dialogs",
  "chromestatusId": 6209615938322432,
  "specUrl": "https://html.spec.whatwg.org/multipage/interactive-elements.html#attr-dialog-closedby",
  "generatedAt": "2026-08-28T00:00:00Z",
  "author": "claude-opus-5-v145-build",
  "assertions": [
    {
      "id": "popover-attribute-is-supported",
      "description": "The overlay type light dismiss applies to by default.",
      "kind": "script",
      "test": "(()=>{try{return 'popover' in document.createElement('div');}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/popover.html#the-popover-attribute"
    },
    {
      "id": "dialog-exposes-closedby",
      "description": "The attribute that lets a dialog choose light dismiss, close requests, both or neither.",
      "kind": "script",
      "test": "(()=>{try{return 'closedBy' in document.createElement('dialog');}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/interactive-elements.html#attr-dialog-closedby"
    },
    {
      "id": "closedby-keywords-resolve-case-insensitively",
      "description": "An enumerated attribute matches keywords ASCII case-insensitively, so ANY is any — and an unrecognised keyword must not be passed through.",
      "kind": "script",
      "test": "(()=>{try{const d=document.createElement('dialog');d.setAttribute('closedby','ANY');const upper=d.closedBy==='any';d.setAttribute('closedby','closerequest');return upper&&d.closedBy==='closerequest';}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/interactive-elements.html#attr-dialog-closedby"
    },
    {
      "id": "the-missing-value-default-depends-on-modality",
      "description": "The detail worth measuring: with no attribute, a dialog opened with showModal() reports closerequest and the same element opened with show() reports none. A single default would make one of the two behaviours unreachable without opting in.",
      "kind": "script",
      "test": "(()=>{try{const d=document.createElement('dialog');d.style.cssText='position:fixed;left:-10000px;width:10px;height:10px;border:0;padding:0';d.textContent='x';document.body.append(d);d.show();const nonModal=d.closedBy;d.close();d.showModal();const modal=d.closedBy;d.close();d.remove();return nonModal==='none'&&modal==='closerequest';}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/interactive-elements.html#attr-dialog-closedby"
    },
    {
      "id": "an-invalid-closedby-does-not-become-any",
      "description": "A typo must not make a dialog easier to dismiss. Whatever the invalid-value default resolves to, it must not be 'any' — that would turn a mistyped attribute into a dialog that vanishes on the next click, losing whatever the user was doing.",
      "kind": "script",
      "test": "(()=>{try{const d=document.createElement('dialog');d.setAttribute('closedby','sometimes');const closed=d.closedBy;d.setAttribute('closedby','');const empty=d.closedBy;return closed!=='any'&&empty!=='any';}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/interactive-elements.html#attr-dialog-closedby"
    },
    {
      "id": "untrusted-input-does-not-light-dismiss",
      "description": "Light dismiss must respond to real input only. If a dispatched pointerdown/pointerup/click sequence closed an open popover, any script on the page could dismiss another script's overlay — and every automated check of this behaviour would be measuring itself rather than the browser.",
      "kind": "script",
      "test": "(async()=>{try{const p=document.createElement('div');p.setAttribute('popover','auto');p.textContent='probe';document.body.append(p);p.showPopover();const opened=p.matches(':popover-open');for(const t of ['pointerdown','pointerup','click']){const C=t==='click'?MouseEvent:PointerEvent;document.body.dispatchEvent(new C(t,{bubbles:true,composed:true,cancelable:true,button:0}));}await new Promise((r)=>setTimeout(r,80));const still=p.matches(':popover-open');try{p.hidePopover();}catch(e){}p.remove();return opened&&still;}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/popover.html#light-dismiss-open-popovers"
    },
    {
      "id": "popover-invalid-value-default-is-manual",
      "description": "The neighbouring attribute resolves a typo to manual — an overlay that stays until told otherwise. Both attributes choose the option that does not disappear unexpectedly, which is the principle behind the light dismiss change itself.",
      "kind": "script",
      "test": "(()=>{try{const e=document.createElement('div');e.setAttribute('popover','sometimes');const invalid=e.popover;e.setAttribute('popover','');return invalid==='manual'&&e.popover==='auto';}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/popover.html#the-popover-attribute"
    },
    {
      "id": "close-watcher-backs-the-close-request",
      "description": "Escape reaching a dialog or popover is a close request, and CloseWatcher is the primitive the platform uses for it. Its presence is what makes closedby=\"closerequest\" a coherent option rather than a special case for one element.",
      "kind": "typeof",
      "test": "CloseWatcher",
      "expect": "function",
      "specSection": "https://html.spec.whatwg.org/multipage/interaction.html#closewatcher"
    }
  ]
}
