{
  "release": "v145",
  "featureSlug": "cookie-store-api-maxage-attribute",
  "chromestatusId": 5190778418757632,
  "specUrl": "https://github.com/whatwg/cookiestore/pull/292",
  "generatedAt": "2026-06-01T08:14:32Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "cookie-store-exists",
      "description": "The CookieStore interface is exposed — the surface that gains the new maxAge option.",
      "kind": "exists",
      "test": "CookieStore"
    },
    {
      "id": "cookie-store-on-window",
      "description": "window.cookieStore is exposed in document contexts, where CookieStoreSetOptions is consumed.",
      "kind": "exists",
      "test": "window.cookieStore"
    },
    {
      "id": "cookie-store-set-is-function",
      "description": "cookieStore.set is a function, the call site that accepts the maxAge attribute.",
      "kind": "typeof",
      "test": "cookieStore.set",
      "expect": "function"
    },
    {
      "id": "cookie-store-set-accepts-maxage",
      "description": "cookieStore.set({ name, value, maxAge }) is accepted without throwing — maxAge is a recognised option in the CookieStoreSetOptions dictionary.",
      "kind": "script",
      "test": "(async()=>{try{await cookieStore.set({name:'cs-conf-maxage',value:'1',maxAge:60});await cookieStore.delete('cs-conf-maxage');return true;}catch(e){return false;}})()",
      "specSection": "https://wicg.github.io/cookie-store/#dictdef-cookiestoresetoptions"
    },
    {
      "id": "cookie-store-delete-is-function",
      "description": "cookieStore.delete is a function — needed to clean up cookies set during testing.",
      "kind": "typeof",
      "test": "cookieStore.delete",
      "expect": "function"
    },
    {
      "id": "cookie-store-get-is-function",
      "description": "cookieStore.get is a function — used to round-trip the value after a maxAge-bounded set.",
      "kind": "typeof",
      "test": "cookieStore.get",
      "expect": "function"
    }
  ]
}
