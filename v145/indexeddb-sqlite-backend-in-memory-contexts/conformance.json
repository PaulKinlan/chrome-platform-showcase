{
  "release": "v145",
  "featureSlug": "indexeddb-sqlite-backend-in-memory-contexts",
  "chromestatusId": 5126896685809664,
  "specUrl": "https://www.w3.org/TR/IndexedDB/",
  "generatedAt": "2026-06-01T08:14:32Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "indexeddb-exposed",
      "description": "indexedDB is exposed on the global — the backend rewrite to SQLite preserves the same Web API surface.",
      "kind": "exists",
      "test": "indexedDB",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "indexed-db-factory-open-is-function",
      "description": "indexedDB.open is a function — the canonical entry point for database creation that the new backend serves.",
      "kind": "typeof",
      "test": "indexedDB.open",
      "expect": "function",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "idb-object-store-put-exists",
      "description": "IDBObjectStore.prototype.put exists — write API behaviour is unchanged by the SQLite backend.",
      "kind": "typeof",
      "test": "IDBObjectStore.prototype.put",
      "expect": "function",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "idb-transaction-prototype-commit",
      "description": "IDBTransaction.prototype.commit exists — explicit commit semantics are supported by the new backend.",
      "kind": "typeof",
      "test": "IDBTransaction.prototype.commit",
      "expect": "function",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "idb-open-roundtrip",
      "description": "Opening and closing a database round-trips — exercises the backend without observing any web-facing change.",
      "kind": "script",
      "test": "(async()=>{try{const req=indexedDB.open('conf-sqlite-bg',1);const db=await new Promise((r,j)=>{req.onsuccess=()=>r(req.result);req.onerror=()=>j(req.error);req.onupgradeneeded=()=>{};});db.close();indexedDB.deleteDatabase('conf-sqlite-bg');return true;}catch(e){return false;}})()",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    }
  ]
}
