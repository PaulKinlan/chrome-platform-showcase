{
  "release": "v145",
  "featureSlug": "introducing-the-origin-api",
  "conceptSlug": "origin-equivalence-tester",
  "chromestatusId": 5095541277065216,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "partial",
      "rationale": "Touches HTML, URL, same-origin from the chromestatus summary but skips well-defined, widely-used, ASCII, same-site — secondary spec surfaces are not exercised."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Live UI is real (button, eventListener, input); controls include \"scheme + default port\"; \"subdomain\"; \"scheme mismatch\" — well past the no-static-card bar."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Feature-index blurb and page H1/lede share the same framing and surface the same API affordances."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "One failure-mode signal is present but most edge cases (errors, unsupported environment, empty/zero inputs) are not demonstrated."
    },
    "references": {
      "state": "fail",
      "rationale": "No chromestatus.com/feature/<id> link found — the self-heal cleanup script cannot recover this folder if the slug ever drifts."
    },
    "design_system": {
      "state": "pass",
      "rationale": "Uses CSS variables, no raw hex in <style>, button overrides set background and color together, no static-card pattern."
    }
  },
  "openQuestions": [
    {
      "title": "Surface the missing API surfaces on origin-equivalence-tester",
      "detail": "The chromestatus summary for Introducing the Origin API introduces well-defined, widely-used, ASCII, same-site but the page does not mention or exercise them. Walk the spec and add a short labelled control or read-out for each, so the concept covers the headline API in full. Spec: https://github.com/whatwg/html/pull/11846",
      "severity": "moderate"
    },
    {
      "title": "Show the failure modes on origin-equivalence-tester",
      "detail": "Right now the page only demonstrates the happy path. Add at least one of: a feature-detection probe with graceful fallback, an error/throw path (empty input, invalid argument), and a boundary case (zero, max, abort).",
      "severity": "moderate"
    },
    {
      "title": "Add the chromestatus reference link to the see-also section",
      "detail": "CLAUDE.md rule #3: every page must include the chromestatus.com/feature/<id> link so the slug-recovery script can recover this folder if the name ever drifts.",
      "severity": "major"
    }
  ],
  "summary": "origin-equivalence-tester covers the happy path of Introducing the Origin API and is interactive enough to clear the bar, but spec coverage and edge cases need filling in — see open questions."
}
