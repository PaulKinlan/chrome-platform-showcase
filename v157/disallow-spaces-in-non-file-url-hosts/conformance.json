{
  "release": "v157",
  "featureSlug": "disallow-spaces-in-non-file-url-hosts",
  "chromestatusId": 5083335148437504,
  "specUrl": "https://url.spec.whatwg.org/#forbidden-host-code-point",
  "generatedAt": "2026-08-27T22:30:00Z",
  "author": "claude-opus-5-v157-build",
  "assertions": [
    {
      "id": "space-in-host-rejected",
      "description": "U+0020 SPACE is a forbidden host code point, so a non-file URL whose host contains one must fail to parse.",
      "kind": "throws",
      "test": "new URL('https://exa mple.com/')",
      "specSection": "https://url.spec.whatwg.org/#forbidden-host-code-point"
    },
    {
      "id": "space-in-websocket-host-rejected",
      "description": "WebSocket URLs go through the same host parser, so a space in a wss:// host is rejected too. This is the Interop 2024 WebSocket case that motivated the change.",
      "kind": "throws",
      "test": "new URL('wss://exa mple.com/socket')",
      "specSection": "https://url.spec.whatwg.org/#concept-host-parser"
    },
    {
      "id": "space-in-file-host-allowed",
      "description": "file:// is exempt: a space in a file URL's host still parses, because file paths legitimately contain spaces.",
      "kind": "script",
      "test": "(()=>{try{new URL('file://exa mple/notes.txt');return true;}catch(e){return false;}})()",
      "specSection": "https://url.spec.whatwg.org/#file-host-state"
    },
    {
      "id": "space-in-path-encoded-not-rejected",
      "description": "A space in the path is legal and serialises to %20. The change is scoped to the host and must not affect path parsing.",
      "kind": "script",
      "test": "new URL('https://example.com/a b').pathname === '/a%20b'",
      "specSection": "https://url.spec.whatwg.org/#path-state"
    },
    {
      "id": "space-in-query-round-trips",
      "description": "A space in the query is legal and decodes back to a space through searchParams.",
      "kind": "script",
      "test": "new URL('https://example.com/?q=a b').searchParams.get('q') === 'a b'",
      "specSection": "https://url.spec.whatwg.org/#query-state"
    },
    {
      "id": "percent-encoded-space-in-host-parses",
      "description": "%20 is a legal host character, so a percent-encoded space still parses. Percent-encoding is therefore not a fix for a host with a space in it: it produces a valid host that does not resolve.",
      "kind": "script",
      "test": "new URL('https://exa%20mple.com/').host === 'exa%20mple.com'",
      "specSection": "https://url.spec.whatwg.org/#concept-opaque-host-parser"
    },
    {
      "id": "tab-removed-before-host-parsing",
      "description": "Tab, LF and CR are stripped from the input before host parsing begins, so they never reach the host and never cause a rejection: 'https://exa\\u0009mple.com/' parses with the host example.com.",
      "kind": "script",
      "test": "(()=>{try{return new URL('https://exa'+String.fromCharCode(9)+'mple.com/').host==='example.com';}catch(e){return false;}})()",
      "specSection": "https://url.spec.whatwg.org/#concept-basic-url-parser"
    },
    {
      "id": "pipe-in-host-rejected",
      "description": "U+007C VERTICAL LINE has always been a forbidden host code point; it anchors the space assertions against a rule that predates this change.",
      "kind": "throws",
      "test": "new URL('https://exa|mple.com/')",
      "specSection": "https://url.spec.whatwg.org/#forbidden-host-code-point"
    }
  ]
}
