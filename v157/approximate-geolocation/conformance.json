{
  "release": "v157",
  "featureSlug": "approximate-geolocation",
  "chromestatusId": 5143609755828224,
  "specUrl": "https://github.com/w3c/geolocation/pull/233",
  "generatedAt": "2026-08-27T21:40:00Z",
  "author": "claude-opus-5-v157-build",
  "assertions": [
    {
      "id": "geolocation-exposed",
      "description": "navigator.geolocation is exposed, which every part of this feature builds on.",
      "kind": "exists",
      "test": "navigator.geolocation",
      "specSection": "https://w3c.github.io/geolocation/#navigator_interface"
    },
    {
      "id": "getcurrentposition-callable",
      "description": "navigator.geolocation.getCurrentPosition is a function.",
      "kind": "typeof",
      "test": "navigator.geolocation.getCurrentPosition",
      "expect": "function",
      "specSection": "https://w3c.github.io/geolocation/#getcurrentposition-method"
    },
    {
      "id": "watchposition-callable",
      "description": "navigator.geolocation.watchPosition is a function.",
      "kind": "typeof",
      "test": "navigator.geolocation.watchPosition",
      "expect": "function",
      "specSection": "https://w3c.github.io/geolocation/#watchposition-method"
    },
    {
      "id": "positionoptions-reads-accuracymode",
      "description": "getCurrentPosition reads the accuracyMode member off the PositionOptions dictionary. A getter on the member fires only when the implementation looks for it, so this distinguishes a shipped option from one that is silently dropped, without needing a position or a permission grant.",
      "kind": "script",
      "test": "(()=>{if(!navigator.geolocation)return false;let read=false;try{navigator.geolocation.getCurrentPosition(()=>{},()=>{},{timeout:1,get accuracyMode(){read=true;return 'approximate';}});}catch(e){return false;}return read;})()",
      "specSection": "https://github.com/w3c/geolocation/pull/233"
    },
    {
      "id": "accuracymode-precise-is-default",
      "description": "Omitting accuracyMode is equivalent to requesting \"precise\", so existing getCurrentPosition calls keep their behaviour. Verified by reading the member off a dictionary that supplies no value: the implementation must not require it.",
      "kind": "script",
      "test": "(()=>{if(!navigator.geolocation)return false;try{navigator.geolocation.getCurrentPosition(()=>{},()=>{},{timeout:1});return true;}catch(e){return false;}})()",
      "specSection": "https://github.com/w3c/geolocation/pull/233"
    },
    {
      "id": "invalid-accuracymode-rejected",
      "description": "accuracyMode is an enum of \"precise\" and \"approximate\". A value outside the enum must make the call throw a TypeError rather than being treated as one of the valid modes.",
      "kind": "throws",
      "test": "navigator.geolocation.getCurrentPosition(()=>{},()=>{},{timeout:1,accuracyMode:'somewhere-in-between'})",
      "specSection": "https://webidl.spec.whatwg.org/#es-enumeration"
    },
    {
      "id": "coordinates-expose-accuracy-radius",
      "description": "GeolocationCoordinates exposes an accuracy radius in metres, which is what a site reads to tell a coarse fix from a fine one.",
      "kind": "script",
      "test": "'accuracy' in GeolocationCoordinates.prototype",
      "specSection": "https://w3c.github.io/geolocation/#dom-geolocationcoordinates-accuracy"
    },
    {
      "id": "geolocation-permission-queryable",
      "description": "The geolocation permission is queryable, so a page can show its state without triggering a prompt.",
      "kind": "script",
      "test": "(async()=>{try{const s=await navigator.permissions.query({name:'geolocation'});return ['granted','denied','prompt'].includes(s.state);}catch(e){return false;}})()",
      "specSection": "https://w3c.github.io/permissions/#dom-permissions-query"
    }
  ]
}
