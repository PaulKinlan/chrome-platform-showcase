{
  "release": "v136",
  "featureSlug": "corner-shaping-corner-shape-superellipse-squircle",
  "conceptSlug": "animated-morpher",
  "chromestatusId": 5357329815699456,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Demonstrates `corner-shape` as an animatable property via :hover transitions (round↔squircle, round↔bevel, squircle↔notch) and an `@keyframes` auto-cycle. The spec explicitly calls out animation as a headline capability."
    },
    "interactive": {
      "state": "pass",
      "rationale": "CSS-driven hover interactivity drives 3 morphing cards; the auto-cycler runs a 6s @keyframes loop. For a CSS feature, CSS-only hover/animation qualifies per CLAUDE.md's CSS bullet."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index promises `hover cards morph round ↔ squircle, round ↔ bevel, squircle ↔ notch. Plus an auto-cycle @keyframes that sweeps four shapes on loop` — page is exactly that."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Three combinations covered but the spec also allows animating between values where one is `superellipse(n)` and the other is a named keyword — that transition's interpolation behaviour isn't shown. Also no demonstration of `prefers-reduced-motion` (animations should respect it)."
    },
    "references": {
      "state": "pass",
      "rationale": "`see also` now cites `https://chromestatus.com/feature/5357329815699456` and the CSS Borders 4 corner-shaping spec section."
    },
    "design_system": {
      "state": "pass",
      "rationale": "36 CSS variable references, no raw hex, no button override mismatch."
    }
  },
  "openQuestions": [
    {
      "title": "Show animation between named keyword and superellipse(n)",
      "detail": "Add a fourth card that morphs from `superellipse(2)` to `superellipse(8)` (continuous N transition) and one that morphs from named `bevel` to `superellipse(0.5)` (mixed). The interpolation algorithm for mixed transitions is the spec's least-documented part.",
      "severity": "moderate"
    },
    {
      "title": "Respect prefers-reduced-motion",
      "detail": "The auto-cycler runs forever. Wrap the animation in `@media (prefers-reduced-motion: no-preference)` so users with reduced-motion preferences see a static state. This is best practice and the demo should model it.",
      "severity": "moderate"
    },
    {
      "title": "Annotate the timing function used",
      "detail": "Different timing functions reveal the morph shape differently. Surface the `transition-timing-function` currently in use and consider adding a select to swap between `linear`, `ease`, `cubic-bezier(...)` so authors see the effect on the curve.",
      "severity": "minor"
    }
  ],
  "summary": "Real CSS-only animation demo that proves `corner-shape` interpolates — exactly the spec's claim — with the required ChromeStatus/CSS Borders references now present. It still needs reduced-motion handling and mixed named↔superellipse transition coverage."
}
