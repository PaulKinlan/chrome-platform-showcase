{
  "release": "v136",
  "featureSlug": "corner-shaping-corner-shape-superellipse-squircle",
  "conceptSlug": "shape-explorer",
  "chromestatusId": 5357329815699456,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Renders every value the spec accepts — `round`, `scoop`, `bevel`, `notch`, `straight`, `squircle`, and arbitrary `superellipse(N)` — with a live exponent slider. Complete coverage of the property's accepted values."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Range slider + select + button with 4 listeners drive a live preview as the user sweeps through values; CSS-driven for a CSS feature."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index promises `Every value the spec accepts ... with a live exponent slider that morphs the curve from circle to square`. Page is exactly that."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "All named values + the superellipse continuum covered, but no demonstration of mixing values per corner (`corner-top-left-shape: scoop; corner-bottom-right-shape: notch`) which is where the API surface gets exotic."
    },
    "references": {
      "state": "fail",
      "rationale": "Page has no chromestatus link in `see also`. Sections list contains `see also` but invariant #3's `https://chromestatus.com/feature/5357329815699456` is missing."
    },
    "design_system": {
      "state": "pass",
      "rationale": "34 CSS variable references, no raw hex, no button override mismatch."
    }
  },
  "openQuestions": [
    {
      "title": "Add the chromestatus reference link",
      "detail": "`see also` is missing the `https://chromestatus.com/feature/5357329815699456` URL required by invariant #3. Add it plus the css-borders-4 corner-shaping spec.",
      "severity": "major"
    },
    {
      "title": "Add a feature-detect probe",
      "detail": "Unlike the sibling pages, this one has no probe. Add `CSS.supports('corner-shape', 'squircle')` with a banner so unsupported browsers know.",
      "severity": "moderate"
    },
    {
      "title": "Add per-corner mixing examples",
      "detail": "The most surprising authoring shape is mixing values on different corners. Add a `mix corners` preset where each corner has a different value to show the longhand surface in one frame.",
      "severity": "moderate"
    },
    {
      "title": "Surface the spec's range for `superellipse()`",
      "detail": "Document the allowed exponent range (0–∞ in theory; effectively 0.1–10 practically) on the slider and what each endpoint looks like, so authors know where the bounds are.",
      "severity": "minor"
    }
  ],
  "summary": "Best-coverage page for the property's value surface — all named values + the continuum — but ships without the chromestatus link or a feature-detect probe, both required by CLAUDE.md and present on sibling pages."
}
