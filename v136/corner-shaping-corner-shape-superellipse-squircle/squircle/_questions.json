{
  "release": "v136",
  "featureSlug": "corner-shaping-corner-shape-superellipse-squircle",
  "conceptSlug": "squircle",
  "chromestatusId": 5357329815699456,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Demonstrates the `corner-shape` property with a live `superellipse(n)` exponent slider that morphs the same border-radius from circle to scoop to notch — the spec's headline curvature continuum."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Working range input with 2 live listeners + a feature-detect probe, driving a real `corner-shape` CSS value as the user drags."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index says `Drag the superellipse curvature and watch the same border-radius produce circle, squircle, scoop, or notch` — page is exactly that."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Slider covers the canonical superellipse range but doesn't show the named-value shortcuts (`squircle`, `scoop`, `bevel`, `notch`, `straight`) the spec also accepts in parallel, nor what happens at exponent values outside the typical 0.5–4 range."
    },
    "references": {
      "state": "pass",
      "rationale": "`see also` cites the chromestatus link and the css-borders-4 corner-shaping anchor."
    },
    "design_system": {
      "state": "pass",
      "rationale": "47 CSS variable references, no raw hex, no button override mismatch."
    }
  },
  "openQuestions": [
    {
      "title": "Pair the slider with named-value snap points",
      "detail": "The spec accepts both `superellipse(n)` and named values (`squircle`, `scoop`, `bevel`, `notch`, `straight`, `round`). Add a row of snap buttons that jump the slider to the equivalent exponent so authors learn the named-value vocabulary alongside the continuum.",
      "severity": "moderate"
    },
    {
      "title": "Show the spec's edge values",
      "detail": "What happens at `superellipse(0)` (straight cut) or very large N (effectively a square)? Either clamp the slider to those endpoints with labels or surface what the browser renders so the spec's range is documented.",
      "severity": "minor"
    },
    {
      "title": "Compare with `border-radius` only",
      "detail": "Add a side-by-side rendering of the same border-radius without `corner-shape` so visitors see what the new property is doing on top of the existing one — currently the demo is solo.",
      "severity": "minor"
    }
  ],
  "summary": "Solid headline-knob demo of the new property with a real live slider and proper references. Pairing the continuum with named-value snap buttons and a `border-radius alone` baseline would convert it from `feel the curvature` to a complete spec teach."
}
