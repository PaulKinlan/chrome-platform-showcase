{
  "release": "v136",
  "featureSlug": "audio-output-devices-api-setpreferredsinkid",
  "conceptSlug": "sink-switcher",
  "chromestatusId": 5098834847072256,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Two synthesised tones share a frame-default `setPreferredSinkId`, with per-element `audio.setSinkId()` shown overriding the default for one track. That precedence is exactly what the spec defines."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Mixing-desk UI with select + 7 listeners drives real per-track audio output device routing. Working app, not a card."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index says `Two synthesised tones, one frame-default sink, and per-element overrides. Watch the frame default route every audio element — then watch audio.setSinkId() beat the default for one track`; page implements this faithfully."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Covers the precedence override well but never demonstrates removing the per-element override (going back to frame default), nor the case where the chosen device is unplugged mid-playback."
    },
    "references": {
      "state": "fail",
      "rationale": "Page has no `references`/`see also` containing the chromestatus link. The sections list shows `what the demo is testing` + `the code` + `see also`, but `see also` does not include the required chromestatus.com/feature/5098834847072256 URL — only sibling links."
    },
    "design_system": {
      "state": "pass",
      "rationale": "35 CSS variable references, no raw hex, no button override mismatch."
    }
  },
  "openQuestions": [
    {
      "title": "Add the chromestatus reference link",
      "detail": "The page's `see also` lists only sibling concept pages. Invariant #3: every concept page must include `https://chromestatus.com/feature/5098834847072256` for slug-cleanup recovery. Add it (and the mediacapture-output spec) to `see also`.",
      "severity": "major"
    },
    {
      "title": "Add a feature-detect probe",
      "detail": "Unlike the sibling pages, this concept has no `typeof navigator.mediaDevices.setPreferredSinkId === 'function'` probe. Add one at the top so users see whether their browser exposes the method.",
      "severity": "moderate"
    },
    {
      "title": "Show clearing a per-element override",
      "detail": "After `audio.setSinkId(...)` an element no longer follows the frame default. Add a `reset to frame default` button per track that calls `setSinkId('')` and confirms the element resumes following the frame setting.",
      "severity": "moderate"
    },
    {
      "title": "Handle device-unplug mid-playback",
      "detail": "Listen for `navigator.mediaDevices.devicechange` and surface a banner when the active sink disappears — the precedence demo silently breaks if the chosen device is removed.",
      "severity": "minor"
    }
  ],
  "summary": "Real mixing-desk-style demo of the per-element-overrides-frame-default precedence rule, but it omits the chromestatus self-heal link and skips the feature-detect probe that the other concepts in this feature ship. Adding both plus a `reset override` action would round it out."
}
