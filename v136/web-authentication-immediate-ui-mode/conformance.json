{
  "release": "v136",
  "featureSlug": "web-authentication-immediate-ui-mode",
  "chromestatusId": 5164322780872704,
  "specUrl": "https://github.com/w3c/webauthn/pull/2291",
  "generatedAt": "2026-06-01T08:03:56Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "public-key-credential-exposed",
      "description": "PublicKeyCredential is exposed.",
      "kind": "exists",
      "test": "PublicKeyCredential",
      "specSection": "https://w3c.github.io/webauthn/#publickeycredential"
    },
    {
      "id": "credentials-get-is-function",
      "description": "navigator.credentials.get is a function — the entry point that now accepts mediation:'immediate'.",
      "kind": "typeof",
      "test": "navigator.credentials.get",
      "expect": "function",
      "specSection": "https://w3c.github.io/webappsec-credential-management/#dom-credentialscontainer-get"
    },
    {
      "id": "get-with-immediate-mediation-returns-promise",
      "description": "navigator.credentials.get({mediation:'immediate'}) returns a Promise — the new mediation value is accepted at the IDL boundary.",
      "kind": "script",
      "test": "(()=>{try{const p=navigator.credentials.get({mediation:'immediate',publicKey:{challenge:new Uint8Array(32)}});return p instanceof Promise;}catch(e){return false;}})()",
      "specSection": "https://github.com/w3c/webauthn/pull/2291"
    },
    {
      "id": "abort-signal-supported",
      "description": "AbortSignal exists — required by WebAuthn get() to cancel an immediate-mode request.",
      "kind": "exists",
      "test": "AbortSignal"
    },
    {
      "id": "is-conditional-mediation-available",
      "description": "PublicKeyCredential.isConditionalMediationAvailable exists — sibling capability detection used alongside immediate-mode detection.",
      "kind": "typeof",
      "test": "PublicKeyCredential.isConditionalMediationAvailable",
      "expect": "function"
    }
  ]
}
