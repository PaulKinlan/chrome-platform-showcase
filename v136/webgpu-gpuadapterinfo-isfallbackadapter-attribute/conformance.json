{
  "release": "v136",
  "featureSlug": "webgpu-gpuadapterinfo-isfallbackadapter-attribute",
  "chromestatusId": 5188955424948224,
  "specUrl": "https://gpuweb.github.io/gpuweb/#dom-gpuadapterinfo-isfallbackadapter",
  "generatedAt": "2026-06-01T08:03:56Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "navigator-gpu-exists",
      "description": "navigator.gpu is exposed.",
      "kind": "exists",
      "test": "navigator.gpu"
    },
    {
      "id": "gpu-adapter-info-exposed",
      "description": "GPUAdapterInfo is exposed as a global interface.",
      "kind": "script",
      "test": "typeof GPUAdapterInfo!=='undefined'",
      "specSection": "https://gpuweb.github.io/gpuweb/#gpuadapterinfo"
    },
    {
      "id": "adapter-info-property",
      "description": "Adapter exposes an `info` accessor whose value is a GPUAdapterInfo.",
      "kind": "script",
      "test": "(async()=>{if(!navigator.gpu)return false;const a=await navigator.gpu.requestAdapter();if(!a)return false;return !!a.info;})()",
      "specSection": "https://gpuweb.github.io/gpuweb/#dom-gpuadapter-info"
    },
    {
      "id": "is-fallback-adapter-attribute",
      "description": "GPUAdapterInfo exposes the isFallbackAdapter boolean (new in v136).",
      "kind": "script",
      "test": "(async()=>{if(!navigator.gpu)return false;const a=await navigator.gpu.requestAdapter();if(!a)return false;return typeof a.info.isFallbackAdapter==='boolean';})()",
      "specSection": "https://gpuweb.github.io/gpuweb/#dom-gpuadapterinfo-isfallbackadapter"
    },
    {
      "id": "request-fallback-adapter-flag",
      "description": "navigator.gpu.requestAdapter accepts a forceFallbackAdapter option without throwing (whose returned adapter would have isFallbackAdapter=true).",
      "kind": "script",
      "test": "(async()=>{if(!navigator.gpu)return false;try{const a=await navigator.gpu.requestAdapter({forceFallbackAdapter:true});return a===null || typeof a.info.isFallbackAdapter==='boolean';}catch(e){return false;}})()",
      "specSection": "https://gpuweb.github.io/gpuweb/#dom-gpurequestadapteroptions-forcefallbackadapter"
    }
  ]
}
