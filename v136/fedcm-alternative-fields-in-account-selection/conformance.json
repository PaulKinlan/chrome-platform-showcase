{
  "release": "v136",
  "featureSlug": "fedcm-alternative-fields-in-account-selection",
  "chromestatusId": 5121180773908480,
  "specUrl": "https://github.com/w3c-fedid/FedCM/pull/718",
  "generatedAt": "2026-06-01T08:03:56Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "identity-credential-exists",
      "description": "IdentityCredential is exposed — the credential type FedCM produces.",
      "kind": "exists",
      "test": "IdentityCredential",
      "specSection": "https://w3c-fedid.github.io/FedCM/#browser-api-identity-credential-interface"
    },
    {
      "id": "credentials-get-is-function",
      "description": "navigator.credentials.get is a function — the entry point that consumes the new account-selection fields.",
      "kind": "typeof",
      "test": "navigator.credentials.get",
      "expect": "function",
      "specSection": "https://www.w3.org/TR/credential-management-1/#dom-credentialscontainer-get"
    },
    {
      "id": "credentials-container-exposed",
      "description": "navigator.credentials is exposed for the FedCM `identity` field.",
      "kind": "exists",
      "test": "navigator.credentials"
    },
    {
      "id": "abort-controller-for-fedcm",
      "description": "AbortController exists — required by FedCM get() so authors can cancel account selection.",
      "kind": "typeof",
      "test": "AbortController",
      "expect": "function"
    },
    {
      "id": "identity-provider-config-options",
      "description": "Calling navigator.credentials.get with an `identity` option returns a Promise — the request is recognised even if it ultimately fails.",
      "kind": "script",
      "test": "(()=>{try{const p=navigator.credentials.get({identity:{providers:[]}});return p instanceof Promise;}catch(e){return false;}})()"
    }
  ]
}
