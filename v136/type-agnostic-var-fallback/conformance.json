{
  "release": "v136",
  "featureSlug": "type-agnostic-var-fallback",
  "chromestatusId": 5128966769475584,
  "specUrl": "https://drafts.csswg.org/css-variables-2/",
  "generatedAt": "2026-06-01T08:03:56Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "var-fallback-untyped-property",
      "description": "var() with a fallback that doesn't validate against the referenced custom property's type is now accepted at parse time.",
      "kind": "css-supports",
      "test": "color: var(--undefined, red)",
      "specSection": "https://drafts.csswg.org/css-variables-2/"
    },
    {
      "id": "var-fallback-different-type",
      "description": "A fallback whose type does not match the value computed for the var is allowed — fallback only fires if the var is unset.",
      "kind": "css-supports",
      "test": "color: var(--x, 10px)",
      "specSection": "https://drafts.csswg.org/css-variables-2/"
    },
    {
      "id": "var-no-fallback-still-parses",
      "description": "var() without a fallback still parses (regression check).",
      "kind": "css-supports",
      "test": "color: var(--undefined-color)",
      "specSection": "https://drafts.csswg.org/css-variables-2/"
    },
    {
      "id": "var-fallback-with-calc",
      "description": "A var() fallback can contain a calc() that uses incompatible types relative to the consumer.",
      "kind": "css-supports",
      "test": "width: var(--w, calc(10px + 0px))",
      "specSection": "https://drafts.csswg.org/css-variables-2/"
    },
    {
      "id": "registered-property-via-cssom",
      "description": "CSS.registerProperty is exposed — registered custom properties are where typed validation traditionally applied; the new lookup is fallback-only.",
      "kind": "typeof",
      "test": "CSS.registerProperty",
      "expect": "function",
      "specSection": "https://drafts.css-houdini.org/css-properties-values-api/#dom-css-registerproperty"
    },
    {
      "id": "var-roundtrip-cssom",
      "description": "A var() declaration with a fallback round-trips through CSSOM cssText.",
      "kind": "script",
      "test": "(()=>{const e=document.createElement('div');e.style.color='var(--undef, red)';return e.style.color.includes('var(');})()"
    }
  ]
}
