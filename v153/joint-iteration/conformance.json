{
  "release": "v153",
  "featureSlug": "joint-iteration",
  "chromestatusId": 6249068542164992,
  "specUrl": "https://tc39.es/proposal-joint-iteration/",
  "generatedAt": "2026-08-28T00:00:00Z",
  "author": "claude-opus-5-v153-build",
  "assertions": [
    {
      "id": "iterator-constructor-exists",
      "description": "Both methods are statics on the Iterator constructor, so its presence is the precondition for the feature.",
      "kind": "typeof",
      "test": "Iterator",
      "expect": "function",
      "specSection": "https://tc39.es/ecma262/#sec-iterator-constructor"
    },
    {
      "id": "iterator-zip-exists",
      "description": "The positional form: an iterable of iterables in, an iterator of arrays out.",
      "kind": "script",
      "test": "(()=>{try{return typeof Iterator.zip==='function';}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-joint-iteration/#sec-iterator.zip"
    },
    {
      "id": "iterator-zipkeyed-exists",
      "description": "The keyed form: an object of iterables in, an iterator of objects out.",
      "kind": "script",
      "test": "(()=>{try{return typeof Iterator.zipKeyed==='function';}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-joint-iteration/#sec-iterator.zipkeyed"
    },
    {
      "id": "zip-defaults-to-shortest",
      "description": "With no options at all, ragged sources truncate to the shortest rather than padding or throwing. A default that padded would silently produce undefined values in every existing call site.",
      "kind": "script",
      "test": "(()=>{try{if(typeof Iterator.zip!=='function')return false;const out=[...Iterator.zip([[1,2,3],['a']])];return out.length===1&&out[0][0]===1&&out[0][1]==='a';}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-joint-iteration/#sec-iterator.zip"
    },
    {
      "id": "zip-rejects-an-unknown-mode",
      "description": "An unrecognised mode is a TypeError, not a silent fallback to the default. This is the difference between a typo you find immediately and one that ships.",
      "kind": "script",
      "test": "(()=>{try{if(typeof Iterator.zip!=='function')return false;[...Iterator.zip([[1],[2]],{mode:'longests'})];return false;}catch(e){return e instanceof TypeError;}})()",
      "specSection": "https://tc39.es/proposal-joint-iteration/#sec-iterator.zip"
    },
    {
      "id": "zip-longest-substitutes-padding",
      "description": "Under longest, a source that has finished contributes its padding entry for every remaining step, and the walk continues until the last source is spent.",
      "kind": "script",
      "test": "(()=>{try{if(typeof Iterator.zip!=='function')return false;const out=[...Iterator.zip([[1,2,3],['a']],{mode:'longest',padding:[0,'z']})];return out.length===3&&out[1][1]==='z'&&out[2][1]==='z'&&out[2][0]===3;}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-joint-iteration/#sec-iterator.zip"
    },
    {
      "id": "zip-strict-throws-at-the-diverging-step",
      "description": "Strict cannot pre-check lengths — an iterator has none — so it yields until the step where the sources stop agreeing and throws there. Two values arrive before the TypeError, which is exactly why a strict consumer must treat the throw as fatal rather than keeping what it already read.",
      "kind": "script",
      "test": "(()=>{try{if(typeof Iterator.zip!=='function')return false;const seen=[];try{for(const t of Iterator.zip([[1,2,3],['a','b']],{mode:'strict'}))seen.push(t);}catch(e){return e instanceof TypeError&&seen.length===2;}return false;}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-joint-iteration/#sec-iterator.zip"
    },
    {
      "id": "zip-closes-abandoned-iterators",
      "description": "The core cleanup guarantee, and the thing hand-rolled zips get wrong: when the walk ends early, the sources still open must be closed, so a generator's finally block runs instead of waiting on the collector.",
      "kind": "script",
      "test": "(()=>{try{if(typeof Iterator.zip!=='function')return false;let closed=false;const long=(function*(){try{for(let i=0;i<10;i++)yield i;}finally{closed=true;}})();for(const t of Iterator.zip([long,['a']])){}return closed;}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-joint-iteration/#sec-iteratorcloseall"
    }
  ]
}
