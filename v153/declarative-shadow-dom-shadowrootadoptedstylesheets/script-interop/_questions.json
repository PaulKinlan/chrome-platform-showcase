{
  "release": "v153",
  "featureSlug": "declarative-shadow-dom-shadowrootadoptedstylesheets",
  "conceptSlug": "script-interop",
  "chromestatusId": 4790543041298432,
  "reviewedAt": "2026-07-26T08:30:00Z",
  "reviewer": "weekly-hardening-agent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Shows the declarative attribute and script sharing one system: two ordered specifiers, the mutable adoptedStyleSheets ObservableArray (push/splice/reverse), and module-map identity via import(specifier, { with: { type: 'css' } })."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Five controls all drive the live sheet table, provenance labels and computed heading colour; the labelled script fallback rebuilds the declarative starting state imperatively, after which every control was exercised end-to-end on Chromium 141."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "The feature-index blurb and the page lede promise exactly the controls and probes the page ships; availability copy states the 153 default, the 148-152 flag paths (ShadowRootAdoptedStyleSheet / DeclarativeCSSModules) and the absence of a dedicated chrome://flags entry."
    },
    "edge_cases": {
      "state": "pass",
      "rationale": "reverse() measures colour before/after and honestly reports when the browser does NOT restyle on reorder (observed on 141 and recorded in the readout instead of claiming the spec outcome); splice() removes the constructed sheet at its actual index so reorders cannot strand it; identity check reports a non-match honestly."
    },
    "references": {
      "state": "pass",
      "rationale": "chromestatus.com/feature/4790543041298432 is linked on every page, plus WHATWG HTML PR #12339, the MSEdge explainer, and web.dev CSS module scripts / DSD background."
    },
    "conformance": {
      "state": "pass",
      "rationale": "New root suite (7 script-kind assertions mirroring the IDL reflection, parser population, ordered-set parsing, and a no-attribute control) executes cleanly on Chromium 141: 6 honest fails + 1 control pass, 0 blocked, no exceptions."
    },
    "runtime_evidence": {
      "state": "partial",
      "rationale": "Full control matrix exercised via CDP on headless Chromium 141 at both viewports, console clean (only the environmental blocked-fonts request and the expected pre-153 modulepreload as=style warning). The SUPPORTED declarative path could not be executed anywhere in this sandbox (141 predates the OT); every supported-path claim is spec-derived and reported by probes that returned honest negatives here."
    },
    "telemetry_health": {
      "state": "pass",
      "rationale": "showcaseTelemetry.assert fires after each primary action; fallback branches use distinct *-fallback ids and never report as feature passes."
    },
    "design_system": {
      "state": "pass",
      "rationale": "All colours are design-system CSS variables; page-local button overrides set background and color together; readouts/verdicts meet AA on stone/ivory backgrounds."
    },
    "accessibility": {
      "state": "pass",
      "rationale": "Native buttons/inputs with accessible names, visible focus, aria-live polite outputs for state changes, scoped table headers, titled iframes, no keyboard traps."
    },
    "mobile_desktop_parity": {
      "state": "pass",
      "rationale": "Verified at 360x740 DPR3 mobile and 1280x800-1800 desktop after the final edit: no horizontal overflow (scrollWidth equals viewport), controls wrap, tap targets >= 44px, fallback banners visible on both classes. Parity record for responsive-support.json must be merged by the orchestrator (build agent is fenced out of top-level files)."
    }
  },
  "openQuestions": [
    {
      "title": "Verify the declarative path on a real Chrome 153 (or 148-152 + flag) build",
      "detail": "Sandbox Chromium 141 predates the DeclarativeCSSModules OT, so every declarative probe correctly returned its honest negative and only the imperative fallbacks exercised live styling. On a 153 build, confirm: parse-time adoption populates adoptedStyleSheets synchronously with placeholder sheets, rules appear when fetches complete, the getComputedStyle effects flip the probes to SUPPORTED, and the conformance suite goes 7/7.",
      "severity": "major"
    },
    {
      "title": "Confirm modulepreload as=\"style\" is consumed (not warned about) on 153",
      "detail": "ChromeStatus guidance says to preload referenced sheets with <link rel=\"modulepreload\" as=\"style\">. Chromium 141 logs an 'invalid as value' warning and an unused-preload warning (expected pre-153; without as=\"style\" it is worse: a hard MIME console error). Verify 153 consumes the preload silently and remove this caveat.",
      "severity": "moderate"
    },
    {
      "title": "Check adoptedStyleSheets reorder-restyle behaviour on Chrome 153",
      "detail": "On Chromium 141, reversing the ObservableArray (in place or by reassignment) did not change the computed tie-breaking winner — the heading stayed emerald with [accent, base]. The page reports this honestly. Verify whether 153 re-evaluates cascade order on reorder and, if it still does not, flag it upstream (possible Blink issue) and update the copy.",
      "severity": "moderate"
    }
  ],
  "guidanceConsulted": [
    {
      "id": "flicker-free-client-side-ab-testing",
      "query": "avoid flash of unstyled content when loading stylesheets for shadow DOM web components",
      "recommendation": "Content that must paint styled should not depend on post-parse script; render-blocking tricks (blocking=render) are the legacy workaround for style-applying scripts.",
      "appliedOrException": "Applied as framing: the FOUC concept contrasts parse-time declarative adoption with post-parse script upgrade, and the copy explains why neither script nor blocking=render hacks are needed once the attribute ships; modulepreload as=\"style\" is used per ChromeStatus guidance to avoid the late module fetch.",
      "evidence": "Timeline readout on /fouc-vs-script-upgrade/: script lane styled 615 ms after parse at the default delay, declarative lane honestly never (Chromium 141)."
    },
    {
      "id": "css-layout",
      "query": "responsive row of buttons and live readout panel without horizontal overflow on narrow mobile",
      "recommendation": "Use wrapping flex/grid for control rows, intrinsic sizing, and let long tokens break (overflow-wrap) instead of fixed widths.",
      "appliedOrException": "Applied: .controls is flex-wrap, the race grid collapses to one column under 720px, tables use table-layout:fixed with overflow-wrap:anywhere, and long inline code tokens got overflow-wrap:anywhere after the 360px pass caught 444-515px scrollWidth overflows on two pages.",
      "evidence": "Post-fix CDP measurement: scrollWidth === clientWidth (360) on all six pages at 360x740 DPR3, 1265 at 1280x800."
    },
    {
      "id": "accessibility",
      "query": "announce dynamic status updates to screen readers with aria-live after button click",
      "recommendation": "Surface dynamic results through native output/live regions tied to the triggering control; keep buttons native with visible focus.",
      "appliedOrException": "Applied: every readout is a <output aria-live=\"polite\">, probe tables are real <table> semantics with scoped headers, buttons are native with :focus-visible outlines and 44px min height, iframes carry title attributes, fallback notes are role=note.",
      "evidence": "DOM inspection during the CDP flows, 2026-07-26."
    }
  ],
  "summary": "Interop demo proving one module map and one mutable array; includes an honest record of a real 141 engine quirk (no restyle on array reorder) surfaced by verification."
}
