{
  "release": "v153",
  "featureSlug": "spell-check-custom-dictionary-api",
  "chromestatusId": 6185007701557248,
  "specUrl": "https://github.com/whatwg/html/pull/12590",
  "generatedAt": "2026-08-28T00:00:00Z",
  "author": "claude-opus-5-v153-build",
  "assertions": [
    {
      "id": "document-exposes-the-custom-dictionary",
      "description": "The feature itself, and the recommended feature test: a browser with spell checking exposes the attribute on the document, and one without must not expose it at all.",
      "kind": "script",
      "test": "(()=>{try{return 'spellCheckCustomDictionary' in document&&Boolean(document.spellCheckCustomDictionary);}catch(e){return false;}})()",
      "specSection": "https://github.com/whatwg/html/pull/12590"
    },
    {
      "id": "the-dictionary-has-addwords-and-removewords",
      "description": "Two methods, both taking a sequence — so a glossary is one call rather than a loop, and a page that changes context can take its terms back out again.",
      "kind": "script",
      "test": "(()=>{try{const d=document.spellCheckCustomDictionary;return typeof d?.addWords==='function'&&typeof d?.removeWords==='function';}catch(e){return false;}})()",
      "specSection": "https://github.com/whatwg/html/pull/12590"
    },
    {
      "id": "the-dictionary-cannot-be-read-back",
      "description": "Write-only by design. An enumerable dictionary would report what a user has been typing and would fingerprint them by what a page had already added, so the absence of any reader is part of the contract rather than an omission.",
      "kind": "script",
      "test": "(()=>{try{const d=document.spellCheckCustomDictionary;if(!d)return false;const names=Object.getOwnPropertyNames(Object.getPrototypeOf(d));return !names.some((n)=>/^(get|list|has|entries|keys|values|words)/i.test(n));}catch(e){return false;}})()",
      "specSection": "https://github.com/whatwg/html/pull/12590"
    },
    {
      "id": "the-attribute-is-the-same-object-each-time",
      "description": "[SameObject]: reading the attribute twice must give the same dictionary, so a page can hold a reference rather than re-reading, and two parts of an application cannot end up talking to different lists.",
      "kind": "script",
      "test": "(()=>{try{return document.spellCheckCustomDictionary!==undefined&&document.spellCheckCustomDictionary===document.spellCheckCustomDictionary;}catch(e){return false;}})()",
      "specSection": "https://webidl.spec.whatwg.org/#SameObject"
    },
    {
      "id": "addwords-accepts-a-sequence",
      "description": "The argument is a list of strings. Calling it with a real glossary must not throw — this is the call every page using the feature will make.",
      "kind": "script",
      "test": "(()=>{try{const d=document.spellCheckCustomDictionary;if(!d)return false;d.addWords(['Kinlan','PROJ-1841','zipKeyed']);d.removeWords(['Kinlan','PROJ-1841','zipKeyed']);return true;}catch(e){return false;}})()",
      "specSection": "https://github.com/whatwg/html/pull/12590"
    },
    {
      "id": "a-secure-context-is-the-precondition",
      "description": "[SecureContext]: the attribute is withheld outside one. Asserted here so that an absent attribute can be attributed to the browser rather than to the page — if this fails, the feature check above proves nothing.",
      "kind": "script",
      "test": "(()=>{try{return globalThis.isSecureContext===true;}catch(e){return false;}})()",
      "specSection": "https://w3c.github.io/webappsec-secure-contexts/"
    },
    {
      "id": "spellcheck-is-still-controllable-per-element",
      "description": "The workaround this feature replaces must keep working: the spellcheck attribute reflects and can be turned off per field. A dictionary is an improvement on that switch, not a replacement for it.",
      "kind": "script",
      "test": "(()=>{try{const t=document.createElement('textarea');t.setAttribute('spellcheck','true');const on=t.spellcheck===true;t.setAttribute('spellcheck','false');return on&&t.spellcheck===false;}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/interaction.html#attr-spellcheck"
    }
  ]
}
