{
  "release": "v153",
  "featureSlug": "iterator-join",
  "chromestatusId": 5095183554314240,
  "specUrl": "https://tc39.es/proposal-iterator-join/",
  "generatedAt": "2026-08-28T00:00:00Z",
  "author": "claude-opus-5-v153-build",
  "assertions": [
    {
      "id": "iterator-prototype-is-reachable",
      "description": "The method is defined on %Iterator.prototype%, which every built-in iterator inherits from and which the Iterator global exposes. Everything else here is read off that object.",
      "kind": "script",
      "test": "(()=>{try{return Object.getPrototypeOf(Object.getPrototypeOf([].values()))===Iterator.prototype;}catch(e){return false;}})()",
      "specSection": "https://tc39.es/ecma262/#sec-%iteratorprototype%-object"
    },
    {
      "id": "iterator-helpers-are-present",
      "description": "join is a terminal operation on the helper pipeline. Without map/filter/take there is nothing for it to terminate, so the helpers are the precondition for the feature being useful at all.",
      "kind": "script",
      "test": "(()=>{try{const p=Iterator.prototype;return ['map','filter','take','drop'].every((n)=>typeof p[n]==='function');}catch(e){return false;}})()",
      "specSection": "https://tc39.es/ecma262/#sec-iterator-helper-objects"
    },
    {
      "id": "iterator-join-exists",
      "description": "The feature itself: a join method on %Iterator.prototype%.",
      "kind": "script",
      "test": "(()=>{try{return typeof Iterator.prototype.join==='function';}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-iterator-join/#sec-iteratorprototype.join"
    },
    {
      "id": "iterator-join-takes-one-argument",
      "description": "The separator is the single declared parameter, matching Array.prototype.join's arity. A different length would mean a different signature than the proposal defines.",
      "kind": "script",
      "test": "(()=>{try{return Iterator.prototype.join.length===1;}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-iterator-join/#sec-iteratorprototype.join"
    },
    {
      "id": "iterator-join-matches-array-join",
      "description": "The whole contract in one check: for a spread of inputs and separators, joining an iterator must produce exactly what joining the equivalent array produces — including the empty-string treatment of null and undefined and the default comma.",
      "kind": "script",
      "test": "(()=>{try{if(typeof Iterator.prototype.join!=='function')return false;const inputs=[[1,2,3],['a',null,'c'],[undefined,undefined],[0,false,''],[],[[1,2],[3]]];const seps=[undefined,'',' — ',7,null];for(const input of inputs){if(input.values().join()!==input.join())return false;for(const sep of seps){if(input.values().join(sep)!==input.join(sep))return false;}}return true;}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-iterator-join/#sec-iteratorprototype.join"
    },
    {
      "id": "array-join-maps-nullish-to-empty-string",
      "description": "The reference behaviour the proposal is defined against, asserted on the platform's own Array.prototype.join so the demos can derive their rules from it rather than restating them.",
      "kind": "script",
      "test": "(()=>{try{return ['a',null,'c'].join()==='a,,c'&&[undefined,undefined].join()===','&&[null].join('-')==='';}catch(e){return false;}})()",
      "specSection": "https://tc39.es/ecma262/#sec-array.prototype.join"
    },
    {
      "id": "join-uses-tostring-so-symbols-throw",
      "description": "Elements are converted with ToString, which has no symbol exemption. A join that returned 'Symbol(s)' would be using String() rather than ToString and would be wrong in exactly the way a hand-written shim usually is.",
      "kind": "script",
      "test": "(()=>{try{[Symbol('s')].join();return false;}catch(e){return e instanceof TypeError;}})()",
      "specSection": "https://tc39.es/ecma262/#sec-tostring"
    }
  ]
}
