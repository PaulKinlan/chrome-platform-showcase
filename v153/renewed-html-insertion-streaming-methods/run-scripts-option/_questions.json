{
  "release": "v153",
  "featureSlug": "renewed-html-insertion-streaming-methods",
  "chromestatusId": 5054329641893888,
  "reviewedAt": "2026-07-26T08:15:00Z",
  "reviewer": "weekly-hardening-agent",
  "conceptSlug": "run-scripts-option",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Demonstrates the exact explainer contract: setHTMLUnsafe default keeps scripts inert, {runScripts:true} in SetHTMLUnsafeOptions opts in, createContextualFragment is the legacy precedent; safe variants never get the option (stated in copy)."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Three lanes insert identical script-bearing markup; verdicts are read from a real window.__rhismMarks marker the inserted script must push — the probe can and does return 'did not run'."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Blurb promises the three-way comparison; the page delivers exactly those three lanes plus a run-all summary."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Covers inert default, option-ignored, option-rejected (older Chromium throws Overload resolution failed — caught and reported as the unsupported signal), and a spec-violation detector if the default ever runs scripts. Streaming script timing (parser-blocking classic vs deferred/module at stream close) is described in the explainer but not demonstrated."
    },
    "references": {
      "state": "pass",
      "rationale": "Page links https://chromestatus.com/feature/5054329641893888 plus the explainer section it demonstrates."
    },
    "conformance": {
      "state": "pass",
      "rationale": "Suite asserts both directions: default-inert (passes today, expectedBrowsers all) and runScripts-executes (fails honestly today, wrapped so the old-engine TypeError reads as a clean fail)."
    },
    "runtime_evidence": {
      "state": "partial",
      "rationale": "Chromium 141 verified all three lanes via ?autorun=1: lane 1 inert (correct), lane 2 TypeError caught and reported unsupported, lane 3 script executed (marker + EXECUTED stamp in DOM), zero console errors. The lane-2 success path needs a flag-enabled 153 build."
    },
    "telemetry_health": {
      "state": "pass",
      "rationale": "showcaseTelemetry.assert() fires after each primary action; unsupported/fallback branches assert under distinct ids (…-fallback / …-illustration) and are never reported as feature passes."
    },
    "design_system": {
      "state": "pass",
      "rationale": "Only design-system CSS variables; emerald success surfaces use --text-black text (5.1:1, AA) after an explicit contrast audit; rose surfaces use --bg-ivory (5.26:1); secondary buttons override background AND color together; no static code-card pattern."
    },
    "accessibility": {
      "state": "pass",
      "rationale": "Verdicts are full-text panels (not colour-only), single polite live status, native buttons with visible focus; lane sandboxes have aria-labels."
    },
    "mobile_desktop_parity": {
      "state": "pass",
      "rationale": "CDP device emulation matrix (mobile 360x740 DPR3 touch + desktop 1280x800): no horizontal overflow (scrollWidth==innerWidth both classes), 44px control heights, visible focus outlines, aria-live status, zero console errors on both classes."
    }
  },
  "openQuestions": [
    {
      "title": "Confirm on flag-enabled 153 that lane 2 executes the inline script after insertion and that execution order ma",
      "detail": "Confirm on flag-enabled 153 that lane 2 executes the inline script after insertion and that execution order matches createContextualFragment's after-insertion semantics.",
      "severity": "major"
    },
    {
      "title": "Extend the lab with an external classic script lane to show the non-parser-blocking fetch behaviour the explai",
      "detail": "Extend the lab with an external classic script lane to show the non-parser-blocking fetch behaviour the explainer calls out.",
      "severity": "minor"
    }
  ],
  "guidanceConsulted": [
    {
      "query": "responsive control panel buttons mobile no horizontal overflow tap targets",
      "id": "forms",
      "recommendation": "Constrain form controls to their container (width/min-width/box-sizing) and keep touch targets comfortably large on mobile.",
      "appliedOrException": "All controls are >=2.75rem (44px) tall; at <=640px controls get width:100%, min-width:0 and box-sizing:border-box so selects/textareas cannot widen the layout viewport. Verified via CDP mobile emulation (360x740 DPR3): documentElement.scrollWidth == innerWidth == 360.",
      "evidence": "scratchpad CDP matrix run 2026-07-26: scrollWidth 360 / innerWidth 360, zero console errors"
    },
    {
      "query": "accessible live region announcing async progress and error states",
      "id": "accessibility",
      "recommendation": "Announce async state changes through a polite live region with text equivalents; keep rapid progress updates out of the live region.",
      "appliedOrException": "Each page has a single aria-live=polite <output> status for state changes; the streaming page keeps per-chunk progress in a separate non-live text line so screen readers are not flooded; verdict panels carry full text, not colour alone."
    },
    {
      "query": "streaming progressive HTML rendering into container without layout shift",
      "id": "defer-rendering-heavy-content",
      "recommendation": "Reserve space for content that arrives late/progressively to avoid layout shift.",
      "appliedOrException": "The streaming stage reserves min-height:10rem so chunk arrival does not shift the controls or status region below it."
    }
  ],
  "summary": "The shoot-out gives a genuinely observable three-way comparison today: inert default and createContextualFragment execution both verified live on Chromium 141, with the runScripts lane reporting the engine's options rejection honestly instead of throwing uncaught (bug found and fixed during build)."
}
