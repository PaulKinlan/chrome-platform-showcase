{
  "release": "v153",
  "featureSlug": "renewed-html-insertion-streaming-methods",
  "chromestatusId": 5054329641893888,
  "reviewedAt": "2026-07-26T08:15:00Z",
  "reviewer": "weekly-hardening-agent",
  "conceptSlug": "trusted-types-parser-options",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Probes TrustedTypePolicy.prototype for createHTMLParserOptions (explainer IDL name) and createParserOptions (ChromeStatus shorthand), labelling the naming discrepancy; the supported path passes the blessed dictionary to setHTMLUnsafe per the explainer flow."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Live probe table plus two real actions: today's createHTML string flow (demonstrably leaves a hostile onerror attribute alive — visible hit counter increments) and the parser-options flow (honest no-op fallback when absent)."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Blurb promises the probe plus the contrast with today's string-transforming createHTML flow; delivered."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Covers trustedTypes-missing (non-Chromium), createPolicy throwing, both candidate method names, and the hostile-markup demonstration without any network request (data: URL image). Default-policy interaction and TrustedHTMLParserOptions round-tripping into streaming methods are not exercisable anywhere yet."
    },
    "references": {
      "state": "pass",
      "rationale": "Page links https://chromestatus.com/feature/5054329641893888 plus the explainer section it demonstrates."
    },
    "conformance": {
      "state": "pass",
      "rationale": "Suite asserts the createHTMLParserOptions prototype membership via a script check that executes cleanly and fails honestly today (verified failing on Chromium 141)."
    },
    "runtime_evidence": {
      "state": "partial",
      "rationale": "Chromium 141 verified via ?autorun=1: probe table renders 2 available / 2 missing surfaces, the string flow inserted hostile markup, the inline script stayed inert AND the surviving onerror handler fired exactly once (hit counter 1, class fired) — demonstrating the real hole. The blessing path is untestable in this sandbox."
    },
    "telemetry_health": {
      "state": "pass",
      "rationale": "showcaseTelemetry.assert() fires after each primary action; unsupported/fallback branches assert under distinct ids (…-fallback / …-illustration) and are never reported as feature passes."
    },
    "design_system": {
      "state": "pass",
      "rationale": "Only design-system CSS variables; emerald success surfaces use --text-black text (5.1:1, AA) after an explicit contrast audit; rose surfaces use --bg-ivory (5.26:1); secondary buttons override background AND color together; no static code-card pattern."
    },
    "accessibility": {
      "state": "pass",
      "rationale": "Probe table uses th scope, results carry text verdicts; hit counter is text content in a labelled element; polite live status; native buttons with visible focus."
    },
    "mobile_desktop_parity": {
      "state": "pass",
      "rationale": "CDP device emulation matrix (mobile 360x740 DPR3 touch + desktop 1280x800): no horizontal overflow (scrollWidth==innerWidth both classes), 44px control heights, visible focus outlines, aria-live status, zero console errors on both classes."
    }
  },
  "openQuestions": [
    {
      "title": "On flag-enabled 153, verify createHTMLParserOptions returns a TrustedHTMLParserOptions object, that the inject",
      "detail": "On flag-enabled 153, verify createHTMLParserOptions returns a TrustedHTMLParserOptions object, that the injected sanitizer strips the onerror attribute (hit counter stays 0), and whether the shipped method name matches the explainer.",
      "severity": "major"
    },
    {
      "title": "Demonstrate the default-policy transform path (createHTMLParserOptions on the 'default' policy) once implement",
      "detail": "Demonstrate the default-policy transform path (createHTMLParserOptions on the 'default' policy) once implemented.",
      "severity": "minor"
    }
  ],
  "guidanceConsulted": [
    {
      "query": "responsive control panel buttons mobile no horizontal overflow tap targets",
      "id": "forms",
      "recommendation": "Constrain form controls to their container (width/min-width/box-sizing) and keep touch targets comfortably large on mobile.",
      "appliedOrException": "All controls are >=2.75rem (44px) tall; at <=640px controls get width:100%, min-width:0 and box-sizing:border-box so selects/textareas cannot widen the layout viewport. Verified via CDP mobile emulation (360x740 DPR3): documentElement.scrollWidth == innerWidth == 360.",
      "evidence": "scratchpad CDP matrix run 2026-07-26: scrollWidth 360 / innerWidth 360, zero console errors"
    },
    {
      "query": "accessible live region announcing async progress and error states",
      "id": "accessibility",
      "recommendation": "Announce async state changes through a polite live region with text equivalents; keep rapid progress updates out of the live region.",
      "appliedOrException": "Each page has a single aria-live=polite <output> status for state changes; the streaming page keeps per-chunk progress in a separate non-live text line so screen readers are not flooded; verdict panels carry full text, not colour alone."
    },
    {
      "query": "streaming progressive HTML rendering into container without layout shift",
      "id": "defer-rendering-heavy-content",
      "recommendation": "Reserve space for content that arrives late/progressively to avoid layout shift.",
      "appliedOrException": "The streaming stage reserves min-height:10rem so chunk arrival does not shift the controls or status region below it."
    }
  ],
  "summary": "The page turns the motivation into something observable today — a trusted-types string flow that provably cannot stop a markup-borne event-handler attribute (hit counter fired once on Chromium 141) — while probing both candidate names for the new blessing method and degrading honestly when neither exists."
}
