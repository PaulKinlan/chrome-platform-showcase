{
  "release": "v134",
  "featureSlug": "explicit-resource-management-async",
  "chromestatusId": 5087324181102592,
  "specUrl": "https://tc39.es/proposal-explicit-resource-management/",
  "generatedAt": "2026-06-01T08:30:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "symbol-async-dispose-exists",
      "description": "Symbol.asyncDispose is a well-known symbol used by `await using`.",
      "kind": "script",
      "test": "typeof Symbol.asyncDispose === 'symbol'"
    },
    {
      "id": "asyncdisposablestack-defined",
      "description": "AsyncDisposableStack is exposed globally.",
      "kind": "exists",
      "test": "window.AsyncDisposableStack"
    },
    {
      "id": "asyncdisposablestack-use",
      "description": "AsyncDisposableStack.prototype.use is a function that registers an asyncly-disposed resource.",
      "kind": "typeof",
      "test": "AsyncDisposableStack.prototype.use",
      "expect": "function"
    },
    {
      "id": "asyncdisposablestack-disposeAsync",
      "description": "AsyncDisposableStack.prototype.disposeAsync returns a Promise that resolves once all resources have been disposed.",
      "kind": "script",
      "test": "(async()=>{const s=new AsyncDisposableStack();let saw=0;s.use({[Symbol.asyncDispose](){saw++;return Promise.resolve();}});await s.disposeAsync();return saw===1;})().then(v=>v).catch(()=>false)"
    },
    {
      "id": "await-using-syntax",
      "description": "`await using` syntax parses (async resource binding).",
      "kind": "script",
      "test": "(()=>{try{new Function('return (async()=>{await using x={async [Symbol.asyncDispose](){}};})()');return true;}catch(e){return false;}})()"
    },
    {
      "id": "suppressed-error-defined",
      "description": "SuppressedError is exposed — disposeAsync wraps disposer errors with it.",
      "kind": "exists",
      "test": "window.SuppressedError"
    }
  ]
}
