{
  "release": "v134",
  "featureSlug": "support-web-locks-api-in-shared-storage",
  "chromestatusId": 5133950203461632,
  "specUrl": "https://wicg.github.io/shared-storage/",
  "generatedAt": "2026-06-01T08:30:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "shared-storage-exists",
      "description": "window.sharedStorage exists — the storage scope the lock guards.",
      "kind": "exists",
      "test": "window.sharedStorage"
    },
    {
      "id": "web-locks-api-exists",
      "description": "navigator.locks (Web Locks API) is exposed and is the lock manager Shared Storage integrates with.",
      "kind": "exists",
      "test": "navigator.locks"
    },
    {
      "id": "web-locks-request-fn",
      "description": "navigator.locks.request is callable from the page side.",
      "kind": "typeof",
      "test": "navigator.locks.request",
      "expect": "function"
    },
    {
      "id": "shared-storage-batchupdate",
      "description": "sharedStorage.batchUpdate is exposed (the recommended atomic-mutation surface that benefits from locks).",
      "kind": "script",
      "test": "(()=>{return 'batchUpdate' in window.sharedStorage || typeof window.sharedStorage.batchUpdate==='function';})()"
    },
    {
      "id": "shared-storage-set",
      "description": "sharedStorage.set is callable — writes guarded by Web Locks when the worklet uses navigator.locks.",
      "kind": "typeof",
      "test": "window.sharedStorage.set",
      "expect": "function"
    },
    {
      "id": "web-locks-query-fn",
      "description": "navigator.locks.query lets the page inspect currently held locks.",
      "kind": "typeof",
      "test": "navigator.locks.query",
      "expect": "function"
    }
  ]
}
