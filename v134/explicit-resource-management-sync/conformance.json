{
  "release": "v134",
  "featureSlug": "explicit-resource-management-sync",
  "chromestatusId": 5071680358842368,
  "specUrl": "https://tc39.es/proposal-explicit-resource-management/",
  "generatedAt": "2026-06-01T08:30:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "symbol-dispose-exists",
      "description": "Symbol.dispose is a well-known symbol used by `using`.",
      "kind": "script",
      "test": "typeof Symbol.dispose === 'symbol'"
    },
    {
      "id": "disposablestack-defined",
      "description": "DisposableStack is exposed globally.",
      "kind": "exists",
      "test": "window.DisposableStack"
    },
    {
      "id": "disposablestack-use",
      "description": "DisposableStack.prototype.use registers a sync-disposable resource.",
      "kind": "typeof",
      "test": "DisposableStack.prototype.use",
      "expect": "function"
    },
    {
      "id": "disposablestack-dispose-runs-lifo",
      "description": "DisposableStack.dispose() runs registered disposers in LIFO order.",
      "kind": "script",
      "test": "(()=>{const s=new DisposableStack();const order=[];s.use({[Symbol.dispose](){order.push('a');}});s.use({[Symbol.dispose](){order.push('b');}});s.dispose();return order.join(',')==='b,a';})()"
    },
    {
      "id": "using-declaration-syntax",
      "description": "`using` declaration syntax parses (sync resource binding).",
      "kind": "script",
      "test": "(()=>{try{new Function('using x={[Symbol.dispose](){}};return x;');return true;}catch(e){return false;}})()"
    },
    {
      "id": "suppressed-error-name",
      "description": "SuppressedError has the expected name property and is constructible.",
      "kind": "script",
      "test": "(()=>{const e=new SuppressedError('e','s','msg');return e.name==='SuppressedError'&&e.error==='e'&&e.suppressed==='s';})()"
    }
  ]
}
