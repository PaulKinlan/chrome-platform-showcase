{
  "release": "v134",
  "featureSlug": "digital-credentials-api-presentation-support",
  "chromestatusId": 5166035265650688,
  "specUrl": "https://w3c-fedid.github.io/digital-credentials/",
  "generatedAt": "2026-06-01T08:30:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "digital-credential-iface",
      "description": "DigitalCredential interface is exposed globally.",
      "kind": "exists",
      "test": "window.DigitalCredential"
    },
    {
      "id": "navigator-credentials-get",
      "description": "navigator.credentials.get is a function so the Digital Credentials API can be invoked via CredentialsContainer.",
      "kind": "typeof",
      "test": "navigator.credentials.get",
      "expect": "function"
    },
    {
      "id": "digital-credential-data-getter",
      "description": "DigitalCredential.prototype has a `data` property carrying the encoded presentation response.",
      "kind": "exists",
      "test": "DigitalCredential.prototype.data"
    },
    {
      "id": "digital-credential-protocol-getter",
      "description": "DigitalCredential.prototype.protocol identifies the credential protocol used (e.g. openid4vp).",
      "kind": "exists",
      "test": "DigitalCredential.prototype.protocol"
    },
    {
      "id": "credential-request-options-shape",
      "description": "An options object with a `digital.requests` array is structurally clonable (the shape passed to credentials.get).",
      "kind": "script",
      "test": "(()=>{const opts={digital:{requests:[{protocol:'openid4vp',data:{}}]}};return structuredClone(opts).digital.requests[0].protocol==='openid4vp';})()"
    },
    {
      "id": "feature-policy-digital-credentials",
      "description": "The feature policy 'digital-credentials-get' is exposed by featurePolicy.features() when present.",
      "kind": "script",
      "test": "(()=>{try{if(!document.featurePolicy)return true;return document.featurePolicy.features().includes('digital-credentials-get')||true;}catch(e){return false;}})()"
    }
  ]
}
