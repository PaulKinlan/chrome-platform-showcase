{
  "release": "v134",
  "featureSlug": "remove-nonstandard-getusermedia-audio-constraints",
  "chromestatusId": 5097536380207104,
  "specUrl": "https://w3c.github.io/mediacapture-main/#media-track-constraints",
  "generatedAt": "2026-06-01T08:30:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "supported-constraints-no-goog-prefix",
      "description": "getSupportedConstraints() does not advertise the legacy googEchoCancellation Chrome-prefixed constraint.",
      "kind": "script",
      "test": "(()=>{const c=navigator.mediaDevices.getSupportedConstraints();return !('googEchoCancellation' in c);})()"
    },
    {
      "id": "supported-constraints-no-goog-autogain",
      "description": "googAutoGainControl is removed from supported constraints.",
      "kind": "script",
      "test": "(()=>{const c=navigator.mediaDevices.getSupportedConstraints();return !('googAutoGainControl' in c);})()"
    },
    {
      "id": "supported-constraints-no-goog-noise",
      "description": "googNoiseSuppression is removed from supported constraints.",
      "kind": "script",
      "test": "(()=>{const c=navigator.mediaDevices.getSupportedConstraints();return !('googNoiseSuppression' in c);})()"
    },
    {
      "id": "standard-echocancellation-kept",
      "description": "The standard echoCancellation constraint is still advertised.",
      "kind": "script",
      "test": "navigator.mediaDevices.getSupportedConstraints().echoCancellation === true"
    },
    {
      "id": "standard-noisesuppression-kept",
      "description": "The standard noiseSuppression constraint is still advertised.",
      "kind": "script",
      "test": "navigator.mediaDevices.getSupportedConstraints().noiseSuppression === true"
    },
    {
      "id": "standard-autogaincontrol-kept",
      "description": "The standard autoGainControl constraint is still advertised.",
      "kind": "script",
      "test": "navigator.mediaDevices.getSupportedConstraints().autoGainControl === true"
    }
  ]
}
