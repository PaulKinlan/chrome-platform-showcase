{
  "release": "v147",
  "featureSlug": "webxr-layers",
  "conceptSlug": "compositor-model",
  "chromestatusId": 6287363875536896,
  "reviewedAt": "2026-06-01T08:30:22Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "partial",
      "rationale": "Page demonstrates the feature's general idea but doesn't surface the named spec API symbols directly."
    },
    "interactive": {
      "state": "fail",
      "rationale": "No real interactive surface \u2014 this reads as a styled code card."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Page covers the promises the feature index makes for this concept."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "No visible edge-case handling \u2014 only the success path is demonstrated."
    },
    "references": {
      "state": "partial",
      "rationale": "Cites a chromestatus link but the id 6287363875536896 returns 404 on the API \u2014 the real feature id is 6634466544058368."
    },
    "design_system": {
      "state": "pass",
      "rationale": "Uses CSS variables throughout, no raw hex, button overrides keep bg+color paired."
    }
  },
  "openQuestions": [
    {
      "title": "Surface more named spec API for webxr-layers",
      "detail": "The spec summary lists API surfaces this concept doesn't currently name on-page. Either rename the controls / readouts to mention them, or add a second concept page that targets the missing surface directly. The reviewer extracted these from the spec: WebXR Layers level 1. Layer types: XRProjectionLayer, XRQuadLayer, XRCylinderLayer, XREquirectLayer, XRCubeLayer. XRWebGLBinding factory methods. XRLayerLayout: mono / stereo / stereo-left-right / stereo-top-bottom. XRLayerQuality: text-optimized / graphics-optimized. Fixed foveation (0-1) on projection layers. nativeProjectionScaleFactor for resolution. XRWebGLSubImage with color, depth/stencil, ...",
      "severity": "moderate"
    },
    {
      "title": "Cover at least one failure mode",
      "detail": "The page currently shows the happy path. Add a UI affordance that drives the feature into a failure mode the spec explicitly calls out (timeout, denied permission, cross-origin block, malformed input) and surface what the browser actually does.",
      "severity": "moderate"
    },
    {
      "title": "Make this concept interactive",
      "detail": "Concept pages must not ship as styled code cards. Add a control surface that actually invokes the API and shows the side effect in-page.",
      "severity": "major"
    },
    {
      "title": "Fix the chromestatus link to the real feature id",
      "detail": "The cited chromestatus id 404s on the API. Replace it with the correct id (see the chromestatus search for the feature name) so the cleanup script and human visitors land on the actual entry.",
      "severity": "moderate"
    }
  ],
  "summary": "Concept needs follow-up work before it really lands. 4 open question(s) queued."
}
