{
  "release": "v143",
  "featureSlug": "allow-more-characters-in-javascript-dom-apis",
  "conceptSlug": "querySelector-exotic",
  "chromestatusId": 6278918763708416,
  "reviewedAt": "2026-06-03T16:38:34Z",
  "reviewer": "codex",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Demonstrates the full round-trip the relaxation enables: createElement with Unicode tag, append, CSS.escape, querySelector, match. Spec-accurate."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Native-support probe, sandbox with editable tag + attribute inputs, nine-tile escape gallery (each clickable), and a separate attribute-selector sandbox — all invoke real DOM APIs."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index promises 'gallery of 9 character sets … with live create/querySelector results and an attribute selector sandbox' — page delivers that count and shape."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Gallery covers nine scripts; never shows what CSS.escape does to leading digits, to whitespace inside names, or what happens when the same tag is appended twice and querySelector returns the first."
    },
    "references": {
      "state": "pass",
      "rationale": "See-also includes the sibling concepts, https://chromestatus.com/feature/6278918763708416, and the DOM namespaces spec."
    },
    "design_system": {
      "state": "partial",
      "rationale": "Most colours from CSS variables; `.result-box.ok { background: #edfdf0 }` and `.result-box.err { background: #fde8e8 }` are raw hex backgrounds outside the documented token set."
    }
  },
  "openQuestions": [
    {
      "title": "Replace raw hex on .result-box.ok / .result-box.err",
      "detail": "Use `var(--accent-emerald)` and `var(--accent-rose)` directly (with --text-black foreground for AA) instead of `#edfdf0` and `#fde8e8` so the design-system tokens stay consistent.",
      "severity": "moderate"
    },
    {
      "title": "Show the CSS.escape-of-a-leading-digit case",
      "detail": "`CSS.escape('1foo')` returns `\\31 foo` — a non-trivial transformation that the gallery never demonstrates. Adding `1abc` (which createElement still rejects) and `abc1` (which it accepts) as gallery tiles would show the asymmetry between what createElement accepts and what CSS.escape can handle.",
      "severity": "minor"
    },
    {
      "title": "Test querySelectorAll with duplicate Unicode tags",
      "detail": "Appending two `<artículo>` elements and using querySelectorAll(CSS.escape('artículo')) would surface the case-sensitivity and selector-list behaviour the spec preserves. Currently the sandbox only ever creates one element so there's no ambiguity to resolve.",
      "severity": "minor"
    }
  ],
  "summary": "Strong full round-trip story: create + escape + select + verify. The required ChromeStatus and DOM spec links are now present; raw-hex result-box backgrounds and a couple of edge-case galleries remain."
}
