{
  "release": "v143",
  "featureSlug": "deprecate-and-remove-xslt",
  "conceptSlug": "xslt-removal",
  "chromestatusId": 4709671889534976,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Per CLAUDE.md's 'Removals / deprecations' guidance: probe for the removed feature + show the recommended replacement. This page does both — probes XSLTProcessor constructor and live-transform, then renders the same data via DOMParser + template literal."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Two probes (constructor + live transform), an editable XML textarea, render button, and live re-render on input. Hands-on."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index promises 'probes whether XSLTProcessor still exists and still runs in this browser, then shows a working DOMParser+template literal replacement' — page delivers."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Catches the 'ctor present but transformToFragment broken' middle state — good. Doesn't probe `<?xml-stylesheet?>` processing instruction loading (which is a separate code path from XSLTProcessor and has its own removal timing), nor the response-MIME-type path."
    },
    "references": {
      "state": "pass",
      "rationale": "See-also links to https://chromestatus.com/feature/4709671889534976 and the WHATWG removal issue."
    },
    "design_system": {
      "state": "partial",
      "rationale": "Most colours from variables, but inline style on .article markup uses raw style attributes (acceptable, not raw hex). The error path uses `color:var(--accent-rose)` inline — fine. No major violations."
    }
  },
  "openQuestions": [
    {
      "title": "Add a probe for `<?xml-stylesheet?>` processing instruction",
      "detail": "The chromestatus blurb mentions both XSLTProcessor and the `<?xml-stylesheet?>` PI as separate removal paths. The page only probes the JS constructor. Add a third probe that loads an XML document with `<?xml-stylesheet href='x.xsl' type='application/xslt+xml'?>` via fetch + DOMParser and checks whether Chrome applies it — that's the path that breaks for legacy RSS / sitemap pages with embedded XSLT links.",
      "severity": "moderate"
    },
    {
      "title": "Surface real-world breakage examples",
      "detail": "The 'who will this break' question is the most important one for visitors. Surface a list of canonical XSLT-using sites and stylesheets (the chromestatus motivation cites several) with a 'check live' button per row that fetches and probes. Even a static curated list would help a lot.",
      "severity": "moderate"
    },
    {
      "title": "Compare DOMParser+template vs Mustache vs lit-html",
      "detail": "The 'replacement' panel is one specific style of templating. A toggle that switches between the inline template-literal approach, a Mustache-like spec, and a lit-html-style tagged template would show that the replacement is a choice of templating library, not a single thing.",
      "severity": "minor"
    },
    {
      "title": "Show the removal-timeline + Chrome version readout",
      "detail": "Display the Chrome version, the chromestatus removal milestone (143), and the current version's stage in the deprecation timeline (deprecated → enterprise reverse-trial → removed). Helps visitors understand 'I am on Chrome N — what should I see here'.",
      "severity": "minor"
    }
  ],
  "summary": "Good probe + replacement structure following CLAUDE.md's removal/deprecation pattern. The biggest gap is the missing `<?xml-stylesheet?>` PI probe — that's the path that breaks for the RSS/sitemap pages most commonly cited as casualties of the removal."
}
