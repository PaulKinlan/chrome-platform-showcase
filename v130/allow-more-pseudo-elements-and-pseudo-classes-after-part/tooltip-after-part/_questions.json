{
  "release": "v130",
  "featureSlug": "allow-more-pseudo-elements-and-pseudo-classes-after-part",
  "conceptSlug": "tooltip-after-part",
  "chromestatusId": 5195333643272192,
  "reviewedAt": "2026-06-02T12:04:12Z",
  "reviewer": "codex-goal-setting",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Uses ::part(trigger)::after plus chained :hover::after and :focus-visible::after rules against a shadow-DOM info-card component."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Cards, tooltip text editing, and the Chrome 130 versus pre-130 mode toggle remain interactive."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "The page still matches its blurb by demonstrating host-generated tooltips with ::part()::after."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "The page shows a pre-130 fallback but still does not explain attr() resolution or probe combinator rejection."
    },
    "references": {
      "state": "pass",
      "rationale": "The ChromeStatus reference now points to the canonical feature id 5195333643272192."
    },
    "design_system": {
      "state": "pass",
      "rationale": "The page continues to use project design tokens for tooltip, button, and panel states."
    }
  },
  "openQuestions": [
    {
      "title": "Surface the attr() resolution subtlety",
      "detail": "attr(data-tip) on a ::part()::after resolves against the shadow part's host attributes — that's a genuinely subtle and useful pattern that the demo uses but never explains. Add a one-paragraph callout next to the editor so readers learn the attribute-lookup model.",
      "severity": "minor"
    },
    {
      "title": "Add ::part()::before example alongside ::part()::after",
      "detail": "::before is also newly chainable and is arguably more useful for icons. Add a single ::before example (e.g. a leading status dot) so the page covers the full ::before/::after pair, not just one side.",
      "severity": "minor"
    },
    {
      "title": "Probe combinators rejection",
      "detail": "Add a small CSS.supports() readout for still-rejected chains so the visitor understands the limit. Combinators after ::part() (e.g. ::part(trigger) > .tip) are the obvious one and would be useful in this exact tooltip scenario, which makes their rejection worth showing.",
      "severity": "minor"
    }
  ],
  "summary": "The tooltip demo remains a real ::part()::after showcase with a working fallback mode, and its ChromeStatus reference now uses the correct feature id."
}
