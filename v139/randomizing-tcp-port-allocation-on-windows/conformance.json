{
  "release": "v139",
  "featureSlug": "randomizing-tcp-port-allocation-on-windows",
  "chromestatusId": 5106900286570496,
  "specUrl": "https://chromestatus.com/feature/5106900286570496",
  "generatedAt": "2026-06-01T08:13:31Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "fetch-still-available",
      "description": "fetch() is exposed — the random port allocation change is invisible at the JS API surface but TCP-backed APIs must continue to work.",
      "kind": "typeof",
      "test": "fetch",
      "expect": "function",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "websocket-still-available",
      "description": "WebSocket is still exposed; new TCP connections it opens now use randomized ephemeral ports on Windows.",
      "kind": "typeof",
      "test": "WebSocket",
      "expect": "function"
    },
    {
      "id": "rtc-peerconnection-still-available",
      "description": "RTCPeerConnection is still exposed.",
      "kind": "exists",
      "test": "RTCPeerConnection"
    },
    {
      "id": "user-agent-is-string",
      "description": "navigator.userAgent is exposed (used to gate Windows-specific behaviour).",
      "kind": "typeof",
      "test": "navigator.userAgent",
      "expect": "string"
    },
    {
      "id": "windows-ua-marker-when-applicable",
      "description": "If the visitor is on Windows, the UA string still contains a 'Windows' substring (the platform this allocation change targets).",
      "kind": "script",
      "test": "(()=>{const ua=navigator.userAgent;if(!/Windows/.test(ua))return true;return /Windows NT/.test(ua);})()"
    },
    {
      "id": "no-port-leak-via-performance-api",
      "description": "Performance API does not expose remote port numbers (no .remotePort field on PerformanceResourceTiming) — the randomized allocation is not observable to JS by spec.",
      "kind": "script",
      "test": "(()=>{const e=performance.getEntriesByType('navigation')[0];if(!e)return true;return !('remotePort' in e);})()"
    }
  ]
}
