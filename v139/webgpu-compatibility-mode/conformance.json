{
  "release": "v139",
  "featureSlug": "webgpu-compatibility-mode",
  "chromestatusId": 6436406437871616,
  "specUrl": "https://www.w3.org/TR/webgpu/#dom-gpu-requestadapter",
  "generatedAt": "2026-06-02T08:08:51Z",
  "author": "conformance-author-codex",
  "assertions": [
    {
      "id": "navigator-gpu-exposed",
      "description": "navigator.gpu is the entry point for requesting a compatibility adapter.",
      "kind": "exists",
      "test": "navigator.gpu",
      "specSection": "https://www.w3.org/TR/webgpu/#gpu-interface"
    },
    {
      "id": "requestadapter-is-function",
      "description": "navigator.gpu.requestAdapter is a function that accepts adapter request options.",
      "kind": "typeof",
      "test": "navigator.gpu.requestAdapter",
      "expect": "function",
      "specSection": "https://www.w3.org/TR/webgpu/#dom-gpu-requestadapter"
    },
    {
      "id": "requestadapter-compatibility-returns-promise",
      "description": "requestAdapter({ featureLevel: 'compatibility' }) returns a Promise.",
      "kind": "script",
      "test": "(()=>{if(!navigator.gpu)return false;return navigator.gpu.requestAdapter({featureLevel:'compatibility'}) instanceof Promise;})()",
      "specSection": "https://www.w3.org/TR/webgpu/#dom-gpu-requestadapter"
    },
    {
      "id": "requestadapter-compatibility-does-not-throw",
      "description": "Passing featureLevel: 'compatibility' is accepted and resolves to null or a GPUAdapter.",
      "kind": "script",
      "test": "(async()=>{try{const a=await navigator.gpu.requestAdapter({featureLevel:'compatibility'});return a===null||typeof a==='object';}catch(_){return false;}})()",
      "specSection": "https://www.w3.org/TR/webgpu/#dom-gpu-requestadapter"
    },
    {
      "id": "gpuadapter-interface-exposed",
      "description": "GPUAdapter is exposed as the object type returned by successful adapter requests.",
      "kind": "exists",
      "test": "GPUAdapter",
      "specSection": "https://www.w3.org/TR/webgpu/#gpuadapter"
    },
    {
      "id": "requestadapter-default-still-returns-promise",
      "description": "Calling requestAdapter() with no featureLevel still returns a Promise for the core path.",
      "kind": "script",
      "test": "(()=>{if(!navigator.gpu)return false;return navigator.gpu.requestAdapter() instanceof Promise;})()",
      "specSection": "https://www.w3.org/TR/webgpu/#dom-gpu-requestadapter"
    }
  ]
}
