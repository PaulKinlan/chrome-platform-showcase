{
  "release": "v139",
  "featureSlug": "secure-payment-confirmation-ux-refresh",
  "chromestatusId": 5206050462236672,
  "specUrl": "https://w3c.github.io/secure-payment-confirmation/",
  "generatedAt": "2026-06-01T08:13:31Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "payment-request-still-exposed",
      "description": "PaymentRequest is exposed — SPC dialogs are launched through it.",
      "kind": "exists",
      "test": "PaymentRequest"
    },
    {
      "id": "payment-response-still-exposed",
      "description": "PaymentResponse is exposed — the refreshed UX still resolves through this response type.",
      "kind": "exists",
      "test": "PaymentResponse"
    },
    {
      "id": "public-key-credential-still-exposed",
      "description": "PublicKeyCredential remains exposed — refreshed SPC still uses WebAuthn-style credentials underneath.",
      "kind": "exists",
      "test": "PublicKeyCredential"
    },
    {
      "id": "spc-construction-accepts-payment-entity-logos",
      "description": "Constructing PaymentRequest with SPC data including a payeeLogo/paymentEntities array does not throw at construction (the refreshed UX adds these optional fields).",
      "kind": "script",
      "test": "(()=>{try{new PaymentRequest([{supportedMethods:'secure-payment-confirmation',data:{credentialIds:[new Uint8Array([1])],challenge:new Uint8Array([1]),rpId:location.hostname,instrument:{displayName:'x',icon:'about:blank'},paymentEntitiesLogos:[{url:'about:blank',label:'a'}],timeout:60000}}],{total:{label:'t',amount:{currency:'USD',value:'1.00'}}});return true;}catch(e){return e.name!=='TypeError';}})()",
      "specSection": "https://w3c.github.io/secure-payment-confirmation/"
    },
    {
      "id": "request-payment-show-method",
      "description": "PaymentRequest.prototype.show is exposed; the refreshed UX is rendered when show() is called.",
      "kind": "typeof",
      "test": "PaymentRequest.prototype.show",
      "expect": "function"
    },
    {
      "id": "android-user-agent-marker-when-applicable",
      "description": "The UX refresh ships on Android Chrome; if the visitor is on Android, the UA reflects it (the platform where the refreshed UX renders).",
      "kind": "script",
      "test": "(()=>{const ua=navigator.userAgent;if(!/Android/.test(ua))return true;return /Mobile|Android/.test(ua);})()"
    }
  ]
}
