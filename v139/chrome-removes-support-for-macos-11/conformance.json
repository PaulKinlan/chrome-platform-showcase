{
  "release": "v139",
  "featureSlug": "chrome-removes-support-for-macos-11",
  "chromestatusId": 4504090090143744,
  "specUrl": "https://chromestatus.com/feature/4504090090143744",
  "generatedAt": "2026-06-01T08:13:31Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "navigator-user-agent-exposed",
      "description": "navigator.userAgent is exposed; on supported (macOS 12+) systems the UA string still parses and includes a version >= 10_15_7 / 12+ for Mac.",
      "kind": "typeof",
      "test": "navigator.userAgent",
      "expect": "string"
    },
    {
      "id": "navigator-platform-still-readable",
      "description": "navigator.platform still resolves to a string on the supported platforms the visitor is running.",
      "kind": "typeof",
      "test": "navigator.platform",
      "expect": "string",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "user-agent-data-when-present",
      "description": "When navigator.userAgentData is present, it surfaces a getHighEntropyValues() method that can report platformVersion (used by sites to handle OS deprecations gracefully).",
      "kind": "script",
      "test": "(()=>{if(!('userAgentData' in navigator))return true;return typeof navigator.userAgentData.getHighEntropyValues === 'function';})()"
    },
    {
      "id": "no-macos-11-build-flag",
      "description": "Chrome no longer ships a macOS 11 build; the runtime has no client hint promising support for that OS family. The visitor's UA mac-version, if present, should be parseable as a major version.",
      "kind": "script",
      "test": "(()=>{const ua=navigator.userAgent;const m=ua.match(/Mac OS X (\\d+)[_\\.]/);return m?Number(m[1])>=10:true;})()"
    },
    {
      "id": "chrome-major-version-at-least-139",
      "description": "If the visitor reports a Chrome major version, it should be at or above 139 (the first release without macOS 11 support).",
      "kind": "script",
      "test": "(()=>{const ua=navigator.userAgent;const m=ua.match(/Chrome\\/(\\d+)/);return m?Number(m[1])>=139:true;})()",
      "expectedBrowsers": ["chrome"]
    }
  ]
}
