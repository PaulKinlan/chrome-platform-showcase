{
  "release": "v139",
  "featureSlug": "web-authentication-immediate-ui-mode",
  "chromestatusId": 5164322780872704,
  "specUrl": "https://github.com/w3c/webauthn/pull/2291",
  "generatedAt": "2026-06-01T08:13:31Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "credentials-container-exposed",
      "description": "navigator.credentials is exposed (the CredentialsContainer where get() lives).",
      "kind": "exists",
      "test": "navigator.credentials"
    },
    {
      "id": "credentials-get-method",
      "description": "navigator.credentials.get is exposed as a function — the entry point for the immediate UI mode.",
      "kind": "typeof",
      "test": "navigator.credentials.get",
      "expect": "function"
    },
    {
      "id": "public-key-credential-exposed",
      "description": "PublicKeyCredential is exposed; the WebAuthn type returned by immediate UI mode.",
      "kind": "exists",
      "test": "PublicKeyCredential"
    },
    {
      "id": "public-key-credential-is-conditional-mediation-available",
      "description": "PublicKeyCredential.isConditionalMediationAvailable is exposed; the conditional mediation path remains available alongside immediate UI.",
      "kind": "script",
      "test": "(()=>{try{return typeof PublicKeyCredential.isConditionalMediationAvailable==='function';}catch(e){return false;}})()"
    },
    {
      "id": "credentials-get-accepts-mediation-immediate",
      "description": "navigator.credentials.get({mediation:'immediate', publicKey:...}) returns a Promise — the API accepts the new 'immediate' mediation value.",
      "kind": "script",
      "test": "(()=>{try{const p=navigator.credentials.get({mediation:'immediate',publicKey:{challenge:new Uint8Array([1])}});p.catch(()=>{});return p&&typeof p.then==='function';}catch(e){return false;}})()",
      "specSection": "https://github.com/w3c/webauthn/pull/2291"
    },
    {
      "id": "password-credential-still-exposed",
      "description": "PasswordCredential is exposed; immediate UI mode covers both passkeys and passwords known to the browser.",
      "kind": "script",
      "test": "'PasswordCredential' in window || 'PasswordCredential' in self"
    }
  ]
}
