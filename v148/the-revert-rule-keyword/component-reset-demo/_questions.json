{
  "release": "v148",
  "featureSlug": "the-revert-rule-keyword",
  "conceptSlug": "component-reset-demo",
  "chromestatusId": 5146458504429568,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "partial",
      "rationale": "Page runs script and renders dynamic UI, but the visible API surface (revert) only partially mirrors what the feature summary calls out \u2014 the headline behaviour isn't unambiguously demonstrated."
    },
    "interactive": {
      "state": "partial",
      "rationale": "Some controls present but the interactive surface is thin (4 buttons / 4 inputs / 4 selects). Hard to call it hands-on."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Concept lede matches the feature-index promise on key terms (author, initial, only, revert, rule, styles, unmodified)."
    },
    "edge_cases": {
      "state": "fail",
      "rationale": "No visible edge case / fallback / error handling \u2014 only the happy path."
    },
    "references": {
      "state": "pass",
      "rationale": "References section cites the chromestatus.com/feature/<id> link directly."
    },
    "design_system": {
      "state": "partial",
      "rationale": "1 button rule(s) override `background` without also overriding `color`. The global button rule sets both, so this leaves the text colour inherited from the dark default (CLAUDE.md rule #6)."
    }
  },
  "openQuestions": [
    {
      "title": "Tighten the link between the demo and the spec's headline behaviour",
      "detail": "Confirm the on-screen interaction maps to a concrete API call from the feature summary on chromestatus: \"The revert-rule keyword rolls back the cascade to the previous rule, similar to how revert-layer rolls back the cascade to the previous layer. For example:\r \r ```\r div { color: green; }\r div { color: \". If the call is being shimmed or simulated, surface that fact in the page and add a real invocation behind a feature-detect.",
      "severity": "moderate"
    },
    {
      "title": "Expand the interactive surface beyond one or two buttons",
      "detail": "Add controls that vary the parameters the spec exposes \u2014 toggles, ranges, selects \u2014 so the user can drive the demo through several distinct states instead of a single one-shot trigger.",
      "severity": "moderate"
    },
    {
      "title": "Add explicit edge case coverage",
      "detail": "Pick one failure mode the spec or chromestatus motivation calls out (unsupported browser, denied permission, empty input, etc.) and surface it as a distinct demo state alongside the happy path.",
      "severity": "moderate"
    },
    {
      "title": "Pair every button background override with a colour override",
      "detail": "Audit the inline `<style>` block: each `button { background: ... }` (or `.foo-btn { background: ... }`) needs an explicit `color: ...` next to it. CLAUDE.md rule #6 documents the light-on-light invisibility bug this causes.",
      "severity": "moderate"
    },
    {
      "title": "Manually verify against the spec text",
      "detail": "Reviewer-v1-subagent based the spec_match verdict on the chromestatus summary (\"The revert-rule keyword rolls back the cascade to the previous rule, similar to how revert-layer rolls back the cascade to the previous layer. For example:\r \r ```\r div { color: green; }\r div { color: revert-rule; /* Effectively green */ }\r \") because reading the full spec for every concept was out of budget. A human or follow-up agent should diff the demo's actual behaviour against https://drafts.csswg.org/css-cascade-5/#revert-rule-keyword to catch anything chromestatus glossed over.",
      "severity": "minor"
    }
  ],
  "summary": "Concept ships ~1069 chars of script across 4 button(s) and 8 other control(s). hits 2 pass / 3 partial / 1 fail across the six rubric criteria."
}
