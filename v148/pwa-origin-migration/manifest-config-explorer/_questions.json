{
  "release": "v148",
  "featureSlug": "pwa-origin-migration",
  "conceptSlug": "manifest-config-explorer",
  "chromestatusId": 5123349239955456,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "fail",
      "rationale": "Page is a static descriptive card with no controls or API invocation \u2014 it does not demonstrate the feature beyond restating it."
    },
    "interactive": {
      "state": "fail",
      "rationale": "No buttons, inputs, or scripts \u2014 the page is a styled reference card, not an interactive demo (CLAUDE.md rule #7)."
    },
    "blurb_alignment": {
      "state": "partial",
      "rationale": "Some overlap with the index blurb (migration, origin) but the framing the index promised is only loosely echoed."
    },
    "edge_cases": {
      "state": "fail",
      "rationale": "No visible edge case / fallback / error handling \u2014 only the happy path."
    },
    "references": {
      "state": "partial",
      "rationale": "ChromeStatus link is present but the page has no dedicated `references` section \u2014 the self-heal contract is fragile."
    },
    "design_system": {
      "state": "fail",
      "rationale": "Static-card pattern \u2014 explicitly forbidden by CLAUDE.md rule #7."
    }
  },
  "openQuestions": [
    {
      "title": "Replace the static card with a working demo",
      "detail": "The page has no buttons, inputs, or runtime script that exercises the API. Per CLAUDE.md rule #7, pure code-snippet cards are unacceptable. Build an interactive surface that actually invokes the feature and surfaces the result.",
      "severity": "major"
    },
    {
      "title": "Tighten the concept lede so it mirrors the feature-index promise",
      "detail": "Index promises: \"Side-by-side view of the old and new origin manifests, highlighting the migration declaration and how each field maps during the migration handshake.\". The concept page lede should open with the same headline behaviour, not a sibling framing.",
      "severity": "minor"
    },
    {
      "title": "Add explicit edge case coverage",
      "detail": "Pick one failure mode the spec or chromestatus motivation calls out (unsupported browser, denied permission, empty input, etc.) and surface it as a distinct demo state alongside the happy path.",
      "severity": "moderate"
    },
    {
      "title": "Wrap references in a labelled `references` section",
      "detail": "Per CLAUDE.md rule #3, the chromestatus link is the self-heal anchor and should sit inside a clearly-labelled `references` section so the recovery script can find it reliably.",
      "severity": "minor"
    },
    {
      "title": "Manually verify against the spec text",
      "detail": "Reviewer-v1-subagent based the spec_match verdict on the chromestatus summary (\"When a user installs a Progressive Web App (PWA), its identity and security context are tightly bound to its web origin, for example, `app.example.com`. This presents a significant challenge for developers who need to change their PWA's ori\") because reading the full spec for every concept was out of budget. A human or follow-up agent should diff the demo's actual behaviour against https://github.com/WICG/manifest-incubations/pull/136 to catch anything chromestatus glossed over.",
      "severity": "minor"
    }
  ],
  "summary": "Page is a static card, which is explicitly out of bounds for the showcase. hits 0 pass / 2 partial / 4 fail across the six rubric criteria."
}
