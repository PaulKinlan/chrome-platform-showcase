{
  "release": "v137",
  "featureSlug": "call-stacks-in-crash-reports-from-unresponsive-web-pages",
  "conceptSlug": "crash-stack",
  "chromestatusId": 4731248572628992,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "partial",
      "rationale": "Page demonstrates the SHAPE of the new crash report body but cannot trigger an actual unresponsiveness crash (that requires the Chrome unresponsive-page detector to fire, which kills the tab) — the demo simulates the payload instead of producing one."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Buttons to mint infinite-loop / deep-recursion / GC-pressure synthesised reports, with the resulting report body rendered in-page."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index blurb describes the headline and the page leads with it."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Missing: cross-origin frame inheriting the report, what happens when the call stack is too deep to serialise, what the report looks like when there is no JS on stack (idle freeze)."
    },
    "references": {
      "state": "pass",
      "rationale": "The see-also section now links to the correct ChromeStatus feature 4731248572628992."
    },
    "design_system": {
      "state": "pass",
      "rationale": "All variables, no raw hex."
    }
  },
  "openQuestions": [
    {
      "title": "Add a real-but-survivable freeze button",
      "detail": "Currently all reports are synthesised. Add a button that runs a 10s blocking loop on a different frame (e.g. an iframe with allow=\"\") so the user can see Chrome's real unresponsive detection fire and a real ReportingObserver event arrive — labelled as `may hang your tab`.",
      "severity": "moderate"
    },
    {
      "title": "Show the idle-freeze case",
      "detail": "When the main thread is frozen by something that is NOT JS (e.g. a synchronous XHR off-thread), the call stack is empty. Synthesise that report shape so authors know what to expect.",
      "severity": "minor"
    }
  ],
  "summary": "Synthesised demo of the report shape is useful; adding a real-but-survivable freeze trigger would close the gap between simulation and spec behaviour."
}
