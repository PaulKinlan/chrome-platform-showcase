{
  "release": "v137",
  "featureSlug": "blob-url-partitioning-fetching-navigation",
  "chromestatusId": 5130361898795008,
  "specUrl": "https://github.com/wanderview/quota-storage-partitioning/blob/main/explainer.md",
  "generatedAt": "2026-06-01T08:40:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "url-createobjecturl-exists",
      "description": "URL.createObjectURL() is the entry point producing partitioned blob: URLs.",
      "kind": "exists",
      "test": "URL.createObjectURL",
      "specSection": "https://w3c.github.io/FileAPI/#dfn-createObjectURL"
    },
    {
      "id": "url-revokeobjecturl-exists",
      "description": "URL.revokeObjectURL() releases a blob: URL within its storage key partition.",
      "kind": "exists",
      "test": "URL.revokeObjectURL",
      "specSection": "https://w3c.github.io/FileAPI/#dfn-revokeObjectURL"
    },
    {
      "id": "createobjecturl-returns-blob-scheme",
      "description": "URL.createObjectURL(blob) returns a string with the blob: scheme.",
      "kind": "script",
      "test": "(()=>{const u=URL.createObjectURL(new Blob(['x']));const ok=u.startsWith('blob:');URL.revokeObjectURL(u);return ok;})()",
      "specSection": "https://w3c.github.io/FileAPI/#dfn-createObjectURL"
    },
    {
      "id": "blob-constructor-exists",
      "description": "The Blob constructor is the source of objects feeding the blob URL store.",
      "kind": "typeof",
      "test": "Blob",
      "expect": "function",
      "specSection": "https://w3c.github.io/FileAPI/#blob-section"
    },
    {
      "id": "fetch-resolves-blob-url-in-same-partition",
      "description": "fetch() can read a blob: URL created in the same agent/storage-key partition.",
      "kind": "script",
      "test": "(async()=>{const u=URL.createObjectURL(new Blob(['hello']));const r=await fetch(u);const t=await r.text();URL.revokeObjectURL(u);return t==='hello';})().then(b=>b).catch(()=>false)",
      "specSection": "https://fetch.spec.whatwg.org/#scheme-fetch"
    },
    {
      "id": "revoked-blob-url-fetch-rejects",
      "description": "Once revokeObjectURL has run, fetching the URL must fail (the entry is gone from the partition store).",
      "kind": "script",
      "test": "(async()=>{const u=URL.createObjectURL(new Blob(['x']));URL.revokeObjectURL(u);try{await fetch(u);return false;}catch(e){return true;}})().then(b=>b).catch(()=>true)",
      "specSection": "https://w3c.github.io/FileAPI/#dfn-revokeObjectURL"
    }
  ]
}
