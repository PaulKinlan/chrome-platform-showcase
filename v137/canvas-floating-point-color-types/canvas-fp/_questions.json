{
  "release": "v137",
  "featureSlug": "canvas-floating-point-color-types",
  "conceptSlug": "canvas-fp",
  "chromestatusId": 5086141338877952,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Requests a floating-point backing store via getContext('2d', { colorSpace, colorType: 'float16' }) — the exact API the spec adds."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Buttons toggle between unorm8 and float16 backing stores and re-render the same scene; visible difference where supported."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index blurb describes a 2D canvas requesting a floating-point backing store; that is what the page does."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Missing: graceful detection on engines that ignore the colorType option, readback via getImageData with the new Float16Array, and out-of-gamut colour clipping."
    },
    "references": {
      "state": "fail",
      "rationale": "Chromestatus link in see-also points at 5053992867495936, not the correct 5086141338877952 — breaks the slug self-heal recovery."
    },
    "design_system": {
      "state": "pass",
      "rationale": "All variables, canvas pixel values are content not design tokens."
    }
  },
  "openQuestions": [
    {
      "title": "Fix the chromestatus reference link",
      "detail": "Replace https://chromestatus.com/feature/5053992867495936 with https://chromestatus.com/feature/5086141338877952.",
      "severity": "major"
    },
    {
      "title": "Add a getImageData(Float16Array) lane",
      "detail": "The new colorType lets `getImageData` return a Float16Array. Add a button that reads back a pixel and prints the raw float values so authors see the wider gamut representation.",
      "severity": "moderate"
    },
    {
      "title": "Add a feature-detection probe with fallback",
      "detail": "If the context option is ignored, the canvas silently uses unorm8 and the visual difference is zero — confusing. Probe with a known-out-of-gamut colour and surface a clear `float16 not supported in this browser` banner when it falls back.",
      "severity": "minor"
    }
  ],
  "summary": "Faithful demo of the new API; broken chromestatus ID and missing Float16Array readback are the two real gaps."
}
