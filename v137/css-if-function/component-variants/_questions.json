{
  "release": "v137",
  "featureSlug": "css-if-function",
  "conceptSlug": "component-variants",
  "chromestatusId": 6313805904347136,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Encodes size/intent/state variants entirely in CSS via if(style(--var: value)) — the canonical `single class + custom property` pattern the feature index calls out."
    },
    "interactive": {
      "state": "pass",
      "rationale": "15 buttons (per probe) drive variant selection across two components and re-style live."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index promises one-class no-JS variants; the page is exactly that."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Missing: an unsupported-arm fallback story, hover/active state composition, dark-mode interaction."
    },
    "references": {
      "state": "pass",
      "rationale": "The see-also section now includes the mandatory ChromeStatus feature 6313805904347136 link."
    },
    "design_system": {
      "state": "fail",
      "rationale": "The JS fallback path bakes raw hex literals (`#fce8e8`, `#e8f8f0`, `#fdf3dc`, `#7a0000`, `#004d28`) instead of design system tokens or page-local custom properties. CLAUDE.md §5 forbids raw hex in concept pages."
    }
  },
  "openQuestions": [
    {
      "title": "Replace raw hex in JS fallbacks",
      "detail": "The variant-fallback maps in JS use raw hex (`#fce8e8`, `#7a0000` etc). Define page-local custom properties (`--danger-bg`, `--danger-fg`) up top and reference them, or use the design system tokens. CLAUDE.md §5.",
      "severity": "moderate"
    },
    {
      "title": "Show dark mode + variants composition",
      "detail": "if(style()) is most useful when combined with if(media()). Add a dark-mode toggle that re-themes the variants through nested if().",
      "severity": "minor"
    }
  ],
  "summary": "Functional variant system; the remaining issues are the raw-hex fallback palette and lack of a dark-mode composition example."
}
