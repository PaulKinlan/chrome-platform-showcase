{
  "release": "v146",
  "featureSlug": "populate-targeturl-during-file-handling",
  "chromestatusId": 5119095123083264,
  "specUrl": "https://wicg.github.io/manifest-incubations/index.html#execute-a-file-handler-launch",
  "generatedAt": "2026-06-01T08:00:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "launchqueue-exists",
      "description": "window.launchQueue is the entry point for receiving file-handling launch events.",
      "kind": "exists",
      "test": "launchQueue"
    },
    {
      "id": "launchqueue-setconsumer-is-function",
      "description": "launchQueue.setConsumer is the registration method for the launch handler.",
      "kind": "typeof",
      "test": "launchQueue.setConsumer",
      "expect": "function"
    },
    {
      "id": "launchparams-interface-exposed",
      "description": "The LaunchParams interface is exposed on the global scope.",
      "kind": "exists",
      "test": "LaunchParams"
    },
    {
      "id": "launchparams-has-targeturl",
      "description": "LaunchParams exposes the targetURL property — the URL the file handler was launched against.",
      "kind": "script",
      "test": "'targetURL' in LaunchParams.prototype"
    },
    {
      "id": "launchparams-has-files",
      "description": "LaunchParams still exposes files (FileSystemHandle[]) — the pre-existing surface that the new targetURL augments.",
      "kind": "script",
      "test": "'files' in LaunchParams.prototype"
    },
    {
      "id": "launchparams-targeturl-stringifies",
      "description": "When populated, LaunchParams.targetURL is a string (URL serialisation) per the file-handling launch algorithm.",
      "kind": "script",
      "test": "(()=>{const d=Object.getOwnPropertyDescriptor(LaunchParams.prototype,'targetURL');return !!d && typeof d.get==='function';})()"
    }
  ]
}
