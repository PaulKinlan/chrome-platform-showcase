{
  "release": "v141",
  "featureSlug": "navigation-api-deferred-commit-precommit-handlers",
  "chromestatusId": 5134734612496384,
  "specUrl": "https://github.com/whatwg/html/pull/10919",
  "generatedAt": "2026-06-01T08:13:08Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "navigation-on-window",
      "description": "window.navigation is the Navigation API entry point that intercept() lives on.",
      "kind": "script",
      "test": "'navigation' in window"
    },
    {
      "id": "navigateevent-interface",
      "description": "NavigateEvent is exposed; it is the event type whose intercept() accepts the new precommitHandler.",
      "kind": "exists",
      "test": "NavigateEvent"
    },
    {
      "id": "navigateevent-intercept-method",
      "description": "NavigateEvent.prototype.intercept exists; the spec extends its options dictionary with precommitHandler.",
      "kind": "script",
      "test": "typeof NavigateEvent!=='undefined' && typeof NavigateEvent.prototype.intercept==='function'"
    },
    {
      "id": "navigation-current-entry",
      "description": "Navigation.currentEntry is exposed, returning the active NavigationHistoryEntry from which deferred-commit transitions start.",
      "kind": "script",
      "test": "(()=>{const n=window.navigation;return !!n && 'currentEntry' in n;})()"
    },
    {
      "id": "navigateevent-canintercept",
      "description": "NavigateEvent.canIntercept is exposed; intercept() and precommit handlers only apply when this is true.",
      "kind": "script",
      "test": "typeof NavigateEvent!=='undefined' && 'canIntercept' in NavigateEvent.prototype"
    },
    {
      "id": "navigation-transition",
      "description": "Navigation.transition is exposed, giving access to the in-flight transition whose committed/finished promises wait on precommitHandler resolution.",
      "kind": "script",
      "test": "(()=>{const n=window.navigation;return !!n && 'transition' in n;})()"
    }
  ]
}
