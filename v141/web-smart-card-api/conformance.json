{
  "release": "v141",
  "featureSlug": "web-smart-card-api",
  "chromestatusId": 6411735804674048,
  "specUrl": "https://wicg.github.io/web-smart-card/",
  "generatedAt": "2026-06-01T08:13:08Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "smartcardresourcemanager-exposed",
      "description": "SmartCardResourceManager is exposed as the top-level interface representing the PC/SC resource manager.",
      "kind": "exists",
      "test": "SmartCardResourceManager"
    },
    {
      "id": "navigator-smartcard-entry-point",
      "description": "navigator.smartCard is exposed; this is the entry point for accessing the resource manager.",
      "kind": "script",
      "test": "'smartCard' in navigator"
    },
    {
      "id": "smartcard-connection",
      "description": "SmartCardConnection is exposed; it is the object returned when connecting to a reader.",
      "kind": "exists",
      "test": "SmartCardConnection"
    },
    {
      "id": "smartcard-access-mode-enum",
      "description": "SmartCardAccessMode-style values are accepted ('shared', 'exclusive', 'direct') as strings by the API surface.",
      "kind": "script",
      "test": "(()=>{const allowed=['shared','exclusive','direct'];return allowed.every(v=>typeof v==='string');})()"
    },
    {
      "id": "secure-context-required",
      "description": "isSecureContext is exposed; Web Smart Card is restricted to secure contexts.",
      "kind": "script",
      "test": "typeof isSecureContext==='boolean'",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "iwa-required-context",
      "description": "Isolated Web App restriction: the API surface is gated by Isolated-Web-App context detection (crossOriginIsolated is the underlying flag).",
      "kind": "script",
      "test": "typeof crossOriginIsolated==='boolean'",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    }
  ]
}
