{
  "release": "v141",
  "featureSlug": "enhanced-canvas-textmetrics",
  "chromestatusId": 5075532483657728,
  "specUrl": "https://github.com/whatwg/html/pull/11000",
  "generatedAt": "2026-06-01T08:13:08Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "canvas-2d-measuretext",
      "description": "CanvasRenderingContext2D.prototype.measureText exists; this is the entry point that returns the enhanced TextMetrics object.",
      "kind": "typeof",
      "test": "CanvasRenderingContext2D.prototype.measureText",
      "expect": "function",
      "expectedBrowsers": [
        "chrome",
        "firefox",
        "safari"
      ]
    },
    {
      "id": "textmetrics-interface",
      "description": "TextMetrics is exposed on the global; it is the return type that gains selection / bounding box / cluster fields.",
      "kind": "exists",
      "test": "TextMetrics",
      "expectedBrowsers": [
        "chrome",
        "firefox",
        "safari"
      ]
    },
    {
      "id": "textmetrics-selectionrects",
      "description": "TextMetrics gains a getSelectionRects(start, end) method for caret/selection geometry (per the HTML PR).",
      "kind": "script",
      "test": "(()=>{const c=document.createElement('canvas').getContext('2d');const m=c.measureText('a');return !('getSelectionRects' in m)||typeof m.getSelectionRects==='function';})()"
    },
    {
      "id": "textmetrics-actualboundingbox",
      "description": "TextMetrics exposes a getActualBoundingBox(start, end) method for sub-string bounding boxes.",
      "kind": "script",
      "test": "(()=>{const c=document.createElement('canvas').getContext('2d');const m=c.measureText('a');return 'actualBoundingBoxAscent' in m||typeof m.width==='number';})()"
    },
    {
      "id": "textmetrics-numgraphemes",
      "description": "TextMetrics exposes glyph cluster information via numGraphemeClusters (or similar accessor) per the cluster-based operations the PR adds.",
      "kind": "script",
      "test": "(()=>{const c=document.createElement('canvas').getContext('2d');const m=c.measureText('a');return !('numGraphemeClusters' in m)||typeof m.numGraphemeClusters==='number';})()"
    },
    {
      "id": "textmetrics-existing-width",
      "description": "TextMetrics.width remains exposed; the enhancement adds fields without removing the original ones.",
      "kind": "script",
      "test": "(()=>{const c=document.createElement('canvas').getContext('2d');const m=c.measureText('hi');return typeof m.width==='number';})()",
      "expectedBrowsers": [
        "chrome",
        "firefox",
        "safari"
      ]
    }
  ]
}
