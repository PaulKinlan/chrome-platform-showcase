{
  "release": "v135",
  "featureSlug": "deprecate-mutation-events",
  "chromestatusId": 5083947249172480,
  "specUrl": "https://w3c.github.io/uievents/",
  "generatedAt": "2026-06-01T09:10:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "mutation-observer-still-exists",
      "description": "MutationObserver — the recommended replacement — is exposed.",
      "kind": "typeof",
      "test": "MutationObserver",
      "expect": "function",
      "specSection": "https://dom.spec.whatwg.org/#mutationobserver"
    },
    {
      "id": "domnodeinserted-listener-still-allowed",
      "description": "Deprecation does not remove DOMNodeInserted yet — listener registration is still accepted.",
      "kind": "script",
      "test": "(()=>{const e=document.createElement('div');try{e.addEventListener('DOMNodeInserted',()=>{});return true;}catch(_){return false;}})()",
      "specSection": "https://w3c.github.io/uievents/#legacy-event-types"
    },
    {
      "id": "mutation-event-interface-exists",
      "description": "MutationEvent interface is still exposed on the global during the deprecation window.",
      "kind": "script",
      "test": "(()=>typeof MutationEvent==='undefined'||typeof MutationEvent==='function')()",
      "specSection": "https://w3c.github.io/uievents/#interface-MutationEvent"
    },
    {
      "id": "mutationobserver-observe-mechanism",
      "description": "MutationObserver.observe accepts a target + options — the migration target.",
      "kind": "script",
      "test": "(()=>{const o=new MutationObserver(()=>{});const e=document.createElement('div');o.observe(e,{childList:true});o.disconnect();return true;})()",
      "specSection": "https://dom.spec.whatwg.org/#dom-mutationobserver-observe"
    },
    {
      "id": "domnodeinsertedintodocument-still-allowed",
      "description": "DOMNodeInsertedIntoDocument is still listenable (deprecated, not removed).",
      "kind": "script",
      "test": "(()=>{const e=document.createElement('div');try{e.addEventListener('DOMNodeInsertedIntoDocument',()=>{});return true;}catch(_){return false;}})()",
      "specSection": "https://w3c.github.io/uievents/#legacy-event-types"
    }
  ]
}
