{
  "release": "v135",
  "featureSlug": "regexp-escape",
  "chromestatusId": 5179154657705984,
  "specUrl": "https://tc39.es/proposal-regex-escaping/",
  "generatedAt": "2026-06-01T09:44:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "regexp-escape-is-function",
      "description": "RegExp.escape is exposed as a static method.",
      "kind": "typeof",
      "test": "RegExp.escape",
      "expect": "function",
      "specSection": "https://tc39.es/proposal-regex-escaping/#sec-regexp.escape"
    },
    {
      "id": "regexp-escape-string-returns-string",
      "description": "RegExp.escape('foo') returns a string.",
      "kind": "script",
      "test": "typeof RegExp.escape('foo')==='string'",
      "specSection": "https://tc39.es/proposal-regex-escaping/#sec-regexp.escape"
    },
    {
      "id": "regexp-escape-handles-metacharacters",
      "description": "RegExp.escape escapes regex metacharacters so the result, used as a pattern, matches the original literal.",
      "kind": "script",
      "test": "(()=>{const lit='a.b+c?';const esc=RegExp.escape(lit);return new RegExp(esc).test(lit);})()",
      "specSection": "https://tc39.es/proposal-regex-escaping/#sec-regexp.escape"
    },
    {
      "id": "regexp-escape-rejects-non-string",
      "description": "RegExp.escape throws a TypeError when called with a non-string argument.",
      "kind": "throws",
      "test": "RegExp.escape(123)",
      "specSection": "https://tc39.es/proposal-regex-escaping/#sec-regexp.escape"
    },
    {
      "id": "regexp-escape-escapes-leading-digit",
      "description": "RegExp.escape escapes characters that have meaning at the start of a class (e.g. digits) so concatenation is safe.",
      "kind": "script",
      "test": "(()=>{const lit='123abc';const esc=RegExp.escape(lit);return new RegExp('^'+esc+'$').test(lit);})()",
      "specSection": "https://tc39.es/proposal-regex-escaping/#sec-regexp.escape"
    }
  ]
}
