{
  "release": "v152",
  "featureSlug": "media-element-pseudo-classes",
  "chromestatusId": 5068277495758848,
  "specUrl": "https://html.spec.whatwg.org/multipage/semantics-other.html#pseudo-classes",
  "generatedAt": "2026-07-15T10:00:00Z",
  "author": "manual-openai-v152-coverage",
  "assertions": [
    {
      "id": "playing-pseudo-class-selector",
      "description": "The :playing pseudo-class parses as a valid selector for audio elements.",
      "kind": "script",
      "test": "(()=>{try{return CSS.supports('selector(audio:playing)');}catch(_){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/semantics-other.html#selector-playing",
      "expectedBrowsers": [
        "chrome",
        "firefox"
      ]
    },
    {
      "id": "paused-pseudo-class-matches-default-media",
      "description": ":paused parses and matches a default paused media element.",
      "kind": "script",
      "test": "(()=>{const audio=document.createElement('audio');try{return CSS.supports('selector(audio:paused)')&&audio.matches(':paused');}catch(_){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/semantics-other.html#selector-paused",
      "expectedBrowsers": [
        "chrome",
        "firefox"
      ]
    },
    {
      "id": "muted-pseudo-class-matches-muted-media",
      "description": ":muted parses and matches a media element with muted set.",
      "kind": "script",
      "test": "(()=>{const audio=document.createElement('audio');audio.muted=true;try{return CSS.supports('selector(audio:muted)')&&audio.matches(':muted');}catch(_){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/semantics-other.html#selector-muted",
      "expectedBrowsers": [
        "chrome",
        "firefox"
      ]
    },
    {
      "id": "seeking-pseudo-class-selector",
      "description": "The :seeking pseudo-class parses as a valid selector for media elements.",
      "kind": "script",
      "test": "(()=>{try{return CSS.supports('selector(video:seeking)');}catch(_){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/semantics-other.html#selector-seeking",
      "expectedBrowsers": [
        "chrome",
        "firefox"
      ]
    },
    {
      "id": "buffering-and-stalled-selectors",
      "description": ":buffering and :stalled parse as valid media-element state selectors.",
      "kind": "script",
      "test": "(()=>{try{return CSS.supports('selector(audio:buffering)')&&CSS.supports('selector(audio:stalled)');}catch(_){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/semantics-other.html#selector-buffering",
      "expectedBrowsers": [
        "chrome"
      ]
    },
    {
      "id": "volume-locked-pseudo-class-selector",
      "description": "The :volume-locked pseudo-class parses as a valid selector for audio elements.",
      "kind": "script",
      "test": "(()=>{try{return CSS.supports('selector(audio:volume-locked)');}catch(_){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/semantics-other.html#selector-volume-locked",
      "expectedBrowsers": [
        "chrome"
      ]
    },
    {
      "id": "stylesheet-with-media-pseudo-classes",
      "description": "Stylesheet rules using media pseudo-classes are accepted rather than dropped.",
      "kind": "script",
      "test": "(()=>{const style=document.createElement('style');style.textContent='audio:playing{outline:1px solid currentColor}audio:paused{opacity:.8}audio:muted{filter:grayscale(1)}';document.head.appendChild(style);const ok=style.sheet.cssRules.length===3;style.remove();return ok;})()",
      "specSection": "https://drafts.csswg.org/selectors-4/#video-state"
    },
    {
      "id": "local-media-fixtures-available",
      "description": "The local CC0 audio and video fixtures used by the demos are served with media content types.",
      "kind": "script",
      "test": "(async()=>{const expected=[['/public/media/media-element-pseudo-classes/flower.webm','video/webm'],['/public/media/media-element-pseudo-classes/flower.mp4','video/mp4'],['/public/media/media-element-pseudo-classes/t-rex-roar.mp3','audio/mpeg']];const rows=await Promise.all(expected.map(async([url,type])=>{const res=await fetch(url,{method:'HEAD'});return res.ok&&(res.headers.get('content-type')||'').startsWith(type);}));return rows.every(Boolean);})()",
      "specSection": "https://drafts.csswg.org/selectors-4/#video-state"
    }
  ]
}
