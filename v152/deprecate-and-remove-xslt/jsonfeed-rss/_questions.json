{
  "release": "v152",
  "featureSlug": "deprecate-and-remove-xslt",
  "conceptSlug": "jsonfeed-rss",
  "chromestatusId": 4709671889534976,
  "reviewedAt": "2026-07-15T10:05:00Z",
  "reviewer": "manual-openai-v152-auto-research",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Targets the biggest XSLT casualty (pretty-printed feed pages) and shows DOMParser + template-string rendering as the modern replacement \u2014 matches CLAUDE.md's 'pair removal with a working example of the recommended replacement' guidance."
    },
    "interactive": {
      "state": "pass",
      "rationale": "URL input, sample buttons, live fetch + parse + render, status panel. 5 controls."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Index promises 'fetches live RSS / Atom and renders it purely with DOMParser + template strings \u2014 the replacement for `<?xml-stylesheet?>`'. Page delivers."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Handles CORS failure with a helpful message; falls back gracefully on no items. Doesn't fall back when description contains HTML-escaped markup that the regex strip leaves messy; doesn't handle JSON Feed (despite the page being called 'jsonfeed-rss'); doesn't show what the old `<?xml-stylesheet?>` path used to render."
    },
    "references": {
      "state": "pass",
      "rationale": "Page contains chromestatus.com/feature/4709671889534976."
    },
    "design_system": {
      "state": "pass",
      "rationale": "Inline style block uses CSS variables; channel title pulled via `style=\"font-family:var(--font-display)\u2026\"` is inline but variable-backed."
    },
    "runtime_evidence": {
      "state": "pass",
      "rationale": "Fresh Chrome DevTools MCP v152 sweep loaded the copied concept, exercised every detected control, found named controls, and reported no post-triage runtime/action failure."
    },
    "conformance": {
      "state": "pass",
      "rationale": "A v152 suite now carries the same feature-ID/spec contract without modifying the immutable source-milestone suite."
    }
  },
  "openQuestions": [
    {
      "title": "Add a real JSON Feed sample to match the slug",
      "detail": "The slug is `jsonfeed-rss` and the index mentions 'fetches live RSS / Atom' \u2014 but the page never renders JSON Feed. Add a JSON Feed (jsonfeed.org) example with a sibling `renderJsonFeed(json)` function so the slug isn't misleading and the page covers both modern replacement formats.",
      "severity": "moderate"
    },
    {
      "title": "Show the pre-removal pretty-print for contrast",
      "detail": "Before XSLT removal, browsers applied a default stylesheet that turned `<rss>` into a readable column. A 'how it used to look' panel (just an image snapshot or a static markup mock-up) would teach what's actually being lost, not just the modern replacement.",
      "severity": "moderate"
    },
    {
      "title": "Strip HTML from `description` correctly",
      "detail": "`.replace(/<[^>]+>/g, ' ')` falls apart on CDATA, on `<![CDATA[<p>\u2026</p>]]>`, and on entity-encoded angle brackets. Use a temporary `document.implementation.createHTMLDocument()` to parse the description fragment and extract textContent \u2014 safer and standards-aligned.",
      "severity": "minor"
    },
    {
      "title": "Add an enclosure / media element probe",
      "detail": "Podcasts ship as RSS with `<enclosure>` elements; that data is invisible in the current rendering. Surface a media-link row when present so the demo handles podcasts as well as text feeds.",
      "severity": "minor"
    }
  ],
  "summary": "Tight DOMParser + template-string renderer for the canonical XSLT casualty. The page name promises JSON Feed but only handles RSS/Atom \u2014 adding a JSON Feed sample (and a 'how it used to look' panel) would round it out."
}
