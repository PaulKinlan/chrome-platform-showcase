{
  "release": "v152",
  "featureSlug": "user-agent-image-replacement-api",
  "chromestatusId": 5076374013476864,
  "specUrl": "https://github.com/explainers-by-googlers/ua-image-replacement",
  "generatedAt": "2026-08-28T00:00:00Z",
  "author": "claude-opus-5-v152-build",
  "assertions": [
    {
      "id": "htmlimageelement-exists",
      "description": "The interface the proposal extends.",
      "kind": "typeof",
      "test": "HTMLImageElement",
      "expect": "function",
      "specSection": "https://html.spec.whatwg.org/multipage/embedded-content.html#htmlimageelement"
    },
    {
      "id": "the-detection-technique-works",
      "description": "The control every other assertion here depends on: an inherited handler property must be findable on an element, and an invented one must not. A prototype-only check would miss every handler an image already has, since HTMLImageElement declares none of its own.",
      "kind": "script",
      "test": "(()=>{try{const img=document.createElement('img');return ('onload' in img)&&!('onuadefinitelynotanevent' in img);}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/webappapis.html#event-handler-idl-attributes"
    },
    {
      "id": "replacedbyuseragent-exists",
      "description": "The feature test the proposal specifies: a read-only boolean saying whether the element is currently showing substitute content. It reflects state, so it exists whether or not a replacement is happening.",
      "kind": "script",
      "test": "(()=>{try{return 'replacedByUserAgent' in document.createElement('img');}catch(e){return false;}})()",
      "specSection": "https://github.com/explainers-by-googlers/ua-image-replacement"
    },
    {
      "id": "both-event-handlers-exist",
      "description": "The two reflected handlers. Both are needed: a page that learns about the start and not the end restores nothing, and one that learns about the end and not the start never adapted in the first place.",
      "kind": "script",
      "test": "(()=>{try{const img=document.createElement('img');return ('onuareplacestart' in img)&&('onuareplaceend' in img);}catch(e){return false;}})()",
      "specSection": "https://github.com/explainers-by-googlers/ua-image-replacement"
    },
    {
      "id": "listeners-for-unknown-events-are-harmless",
      "description": "A page can attach both listeners unconditionally, because addEventListener accepts any name and an event that never fires costs nothing. That is what lets the adaptation code ship without a feature branch around it.",
      "kind": "script",
      "test": "(()=>{try{const img=document.createElement('img');let called=false;img.addEventListener('uareplacestart',()=>{called=true;});img.dispatchEvent(new Event('uareplacestart'));return called;}catch(e){return false;}})()",
      "specSection": "https://dom.spec.whatwg.org/#dom-eventtarget-addeventlistener"
    },
    {
      "id": "an-image-reveals-nothing-else-about-its-pixels",
      "description": "The argument for the feature, asserted as a contract: the observable properties of a loaded image describe what was requested, not what is displayed. src, currentSrc and complete are all about the request — so if they could change under a replacement, a page would have a cheaper way to notice than an event.",
      "kind": "script",
      "test": "(async()=>{try{const img=document.createElement('img');img.src='/public/media/jpeg-xl/image.avif';await new Promise((resolve,reject)=>{img.addEventListener('load',resolve,{once:true});img.addEventListener('error',reject,{once:true});setTimeout(reject,4000);});return img.complete===true&&img.src.endsWith('/public/media/jpeg-xl/image.avif')&&img.currentSrc.endsWith('/public/media/jpeg-xl/image.avif')&&img.naturalWidth>0;}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/embedded-content.html#dom-img-currentsrc"
    }
  ]
}
