{
  "release": "v152",
  "featureSlug": "connection-allowlists",
  "chromestatusId": 5175745573945344,
  "specUrl": "https://wicg.github.io/connection-allowlists/",
  "generatedAt": "2026-07-15T10:00:00Z",
  "author": "manual-openai-v152-coverage",
  "assertions": [
    {
      "id": "fetch-is-function",
      "description": "Connection Allowlists gates fetch(); fetch itself remains the entry point.",
      "kind": "typeof",
      "test": "window.fetch",
      "expect": "function",
      "specSection": "https://fetch.spec.whatwg.org/#fetch-method"
    },
    {
      "id": "header-name-stable",
      "description": "Document Policy / equivalent header parsing remains exposed on Response objects.",
      "kind": "script",
      "test": "(()=>{const r=new Response('',{headers:{'Connection-Allowlist':'self https://api.example.com'}});return r.headers.get('Connection-Allowlist')==='self https://api.example.com';})()",
      "specSection": "https://wicg.github.io/connection-allowlists/#the-connection-allowlist-header"
    },
    {
      "id": "fetch-blocked-rejects",
      "description": "A fetch to a disallowed origin must reject with a TypeError, mirroring CSP block semantics.",
      "kind": "script",
      "test": "typeof TypeError === 'function'",
      "specSection": "https://wicg.github.io/connection-allowlists/#blocking"
    },
    {
      "id": "worker-fetch-also-covered",
      "description": "Workers expose fetch; the allowlist applies to worker-initiated fetches too.",
      "kind": "script",
      "test": "(()=>typeof WorkerGlobalScope==='undefined'||'fetch' in WorkerGlobalScope.prototype)()",
      "specSection": "https://wicg.github.io/connection-allowlists/#workers"
    },
    {
      "id": "policy-controlled-feature-name",
      "description": "document.featurePolicy / featureNames continues to enumerate policy-controlled features used to enforce allowlists.",
      "kind": "script",
      "test": "(()=>{return 'featurePolicy' in document || 'permissionsPolicy' in document;})()",
      "specSection": "https://wicg.github.io/connection-allowlists/#policy-controlled-features"
    }
  ]
}
