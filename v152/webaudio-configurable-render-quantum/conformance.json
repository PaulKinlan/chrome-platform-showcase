{
  "release": "v152",
  "featureSlug": "webaudio-configurable-render-quantum",
  "chromestatusId": 5078190552907776,
  "specUrl": "https://webaudio.github.io/web-audio-api/",
  "generatedAt": "2026-07-12T10:00:00Z",
  "author": "autoresearch-v152-v153",
  "assertions": [
    {
      "id": "audiocontext-rendersize-exists",
      "description": "AudioContext exposes a renderSize property.",
      "kind": "script",
      "test": "(()=>{try{const c=new OfflineAudioContext(2,44100,44100);return typeof c.renderSize!=='undefined'||'renderSize' in c}catch(e){return false}})()",
      "specSection": "https://webaudio.github.io/web-audio-api/#dom-audiocontext-rendersize"
    },
    {
      "id": "audiocontext-rendersize-settable",
      "description": "renderSize can be set to a numeric value (e.g. 128, 256, 512).",
      "kind": "script",
      "test": "(()=>{try{const c=new OfflineAudioContext(2,44100,44100);c.renderSize=128;return c.renderSize===128}catch(e){return false}})()",
      "specSection": "https://webaudio.github.io/web-audio-api/#dom-audiocontext-rendersize"
    },
    {
      "id": "audiocontext-rendersize-throws-invalid",
      "description": "Setting renderSize to 0 or a non-power-of-2 throws or rejects.",
      "kind": "script",
      "test": "(()=>{try{const c=new OfflineAudioContext(2,44100,44100);c.renderSize=0;return false}catch(e){return true}})()",
      "specSection": "https://webaudio.github.io/web-audio-api/#dom-audiocontext-rendersize"
    },
    {
      "id": "offlineaudiocontext-rendersize",
      "description": "OfflineAudioContext also supports renderSize for offline rendering at custom quantum.",
      "kind": "script",
      "test": "(()=>{try{const c=new OfflineAudioContext({length:4096,sampleRate:44100,renderSize:256});return c.renderSize===256}catch(e){return false}})()",
      "specSection": "https://webaudio.github.io/web-audio-api/#dom-offlineaudiocontext-offlineaudiocontext"
    },
    {
      "id": "default-rendersize-128",
      "description": "The default render quantum remains 128 frames when renderSize is not specified.",
      "kind": "script",
      "test": "(()=>{try{const c=new OfflineAudioContext(2,44100,44100);const d=c.renderSize;return d===128||d===undefined}catch(e){return false}})()",
      "specSection": "https://webaudio.github.io/web-audio-api/#dom-audiocontext-rendersize"
    }
  ]
}
