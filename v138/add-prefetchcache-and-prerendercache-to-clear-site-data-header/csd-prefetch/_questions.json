{
  "release": "v138",
  "featureSlug": "add-prefetchcache-and-prerendercache-to-clear-site-data-header",
  "conceptSlug": "csd-prefetch",
  "chromestatusId": 4755745652080640,
  "reviewedAt": "2026-06-05T22:33:39Z",
  "reviewer": "manual-codex-devtools-mcp",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Surfaces the two new tokens and now models the spec's special-case where `cache` or `*` implicitly clears `prefetchCache`."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Real checkboxes drive a live header string and an impact table; this is meaningfully past a static-card."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "The feature-index blurb promises a logout-flow builder that shows the assembled header and resulting state — the page delivers exactly that."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "The cache-to-prefetchCache and wildcard implicit-clearing cases are covered, with unaffected rows still visible, but cross-origin scope and active-prerender skip behaviour remain unmodeled."
    },
    "references": {
      "state": "pass",
      "rationale": "Links to https://chromestatus.com/feature/4755745652080640 in the see-also block."
    },
    "design_system": {
      "state": "pass",
      "rationale": "Uses CSS variables throughout, no static-card pattern, accent-emerald and accent-rose colours are paired with --bg-ivory text for contrast."
    }
  },
  "openQuestions": [
    {
      "title": "Add a cross-origin scenario toggle",
      "detail": "The spec confines clearing to the response origin. A toggle that lets the user pick whether the simulated request was same-origin or cross-origin would surface the rule that other origins' prefetches survive — currently the page implies the clear is global.",
      "severity": "minor"
    },
    {
      "title": "Add the active-prerender exception",
      "detail": "The spec says active (currently-prerendering) traversables are skipped by the clear. The page treats `prerenderCache` as a single yes/no — surface the case where a prerender is in flight and the clear leaves it alone (or queues until activation).",
      "severity": "minor"
    }
  ],
  "summary": "Re-tested with chrome-devtools-mcp in Chrome 148. Before the fix, selecting only `cache` rendered `prefetchCache` as untouched. After the fix, `cache` keeps the header as `Clear-Site-Data: \"cache\"`, marks both HTTP cache and `prefetchCache` cleared, and labels the prefetch row as implicit from `cache`; `*` clears every row and labels the implicit prefetch rule; direct `prefetchCache` selection still only clears that row. The feature conformance route reported 5 PASS / 0 FAIL with no console errors. Remaining gaps are cross-origin scoping and the active-prerender exception."
}
