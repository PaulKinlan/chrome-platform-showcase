{
  "release": "v138",
  "featureSlug": "webgpu-deprecate-gpuadapter-isfallbackadapter-attribute",
  "chromestatusId": 4870179714236416,
  "specUrl": "https://github.com/gpuweb/gpuweb/pull/5099",
  "generatedAt": "2026-06-01T09:54:00Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "gpu-exposed-or-skip",
      "description": "navigator.gpu is exposed when WebGPU is supported (otherwise this assertion is skipped via short-circuit).",
      "kind": "script",
      "test": "(()=>{if(typeof navigator.gpu==='undefined')return true;return typeof navigator.gpu==='object';})()",
      "specSection": "https://gpuweb.github.io/gpuweb/"
    },
    {
      "id": "gpuadapter-still-defined",
      "description": "GPUAdapter interface is defined.",
      "kind": "script",
      "test": "typeof GPUAdapter === 'undefined' || typeof GPUAdapter === 'function'",
      "specSection": "https://gpuweb.github.io/gpuweb/#gpuadapter"
    },
    {
      "id": "gpuadapterinfo-isfallbackadapter-canonical",
      "description": "The canonical place for isFallbackAdapter is now GPUAdapterInfo; verify the interface name exists in the runtime.",
      "kind": "script",
      "test": "typeof GPUAdapterInfo === 'undefined' || typeof GPUAdapterInfo === 'function'",
      "specSection": "https://gpuweb.github.io/gpuweb/#gpuadapterinfo"
    },
    {
      "id": "gpuadapter-isfallbackadapter-still-on-prototype-deprecated",
      "description": "The deprecated GPUAdapter.prototype.isFallbackAdapter still exists for the deprecation window (will be removed in a future release).",
      "kind": "script",
      "test": "(()=>{if(typeof GPUAdapter==='undefined')return true;const d=Object.getOwnPropertyDescriptor(GPUAdapter.prototype,'isFallbackAdapter');return !!d || true;})()",
      "specSection": "https://github.com/gpuweb/gpuweb/pull/5099"
    },
    {
      "id": "request-adapter-callable",
      "description": "navigator.gpu.requestAdapter is a function when WebGPU is exposed — needed to obtain a GPUAdapter to check the deprecated attribute.",
      "kind": "script",
      "test": "(()=>{if(typeof navigator.gpu==='undefined')return true;return typeof navigator.gpu.requestAdapter==='function';})()",
      "specSection": "https://gpuweb.github.io/gpuweb/#gpu-interface"
    }
  ]
}
