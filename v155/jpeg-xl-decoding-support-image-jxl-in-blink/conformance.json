{
  "release": "v155",
  "featureSlug": "jpeg-xl-decoding-support-image-jxl-in-blink",
  "chromestatusId": 5114042131808256,
  "specUrl": "https://www.iso.org/standard/85066.html",
  "generatedAt": "2026-08-28T01:00:00Z",
  "author": "claude-opus-5-v155-build",
  "assertions": [
    {
      "id": "createimagebitmap-available",
      "description": "createImageBitmap is the decode entry point every probe here uses.",
      "kind": "typeof",
      "test": "createImageBitmap",
      "expect": "function",
      "specSection": "https://html.spec.whatwg.org/multipage/imagebitmap-and-animations.html#dom-createimagebitmap"
    },
    {
      "id": "jxl-fixture-served-as-image-jxl",
      "description": "The fixture is served with Content-Type image/jxl. A mislabelled response would make the <picture> negotiation in these demos meaningless, so this checks the harness before it checks the browser.",
      "kind": "script",
      "test": "(async()=>{try{const r=await fetch('/public/media/jpeg-xl/image.jxl',{method:'HEAD'});return (r.headers.get('content-type')||'').includes('image/jxl');}catch(e){return false;}})()",
      "specSection": "https://www.rfc-editor.org/rfc/rfc9694.html"
    },
    {
      "id": "jxl-decodes-to-a-bitmap",
      "description": "A real JPEG XL file decodes to an ImageBitmap of its true dimensions (960x600). This is the feature: no registry to query, only a decode that works.",
      "kind": "script",
      "test": "(async()=>{try{const r=await fetch('/public/media/jpeg-xl/image.jxl');const b=await createImageBitmap(await r.blob());const ok=b.width===960&&b.height===600;b.close();return ok;}catch(e){return false;}})()",
      "specSection": "https://www.iso.org/standard/85066.html"
    },
    {
      "id": "jxl-decodes-to-real-pixels",
      "description": "Decoding must produce varying pixels, not an opaque or empty bitmap of the right size. Drawing to a canvas and reading it back is the only way to tell those apart.",
      "kind": "script",
      "test": "(async()=>{try{const r=await fetch('/public/media/jpeg-xl/image.jxl');const b=await createImageBitmap(await r.blob());const c=document.createElement('canvas');c.width=8;c.height=8;const x=c.getContext('2d',{willReadFrequently:true});x.drawImage(b,0,0,8,8);b.close();const d=x.getImageData(0,0,8,8).data;const colours=new Set();for(let i=0;i<d.length;i+=4)colours.add(d[i]+','+d[i+1]+','+d[i+2]);return colours.size>1;}catch(e){return false;}})()",
      "specSection": "https://www.iso.org/standard/85066.html"
    },
    {
      "id": "jxl-loads-in-an-img-element",
      "description": "The <img> path must work too, not only createImageBitmap: that is how a page actually uses an image.",
      "kind": "script",
      "test": "(async()=>{try{return await new Promise((res)=>{const i=new Image();i.onload=()=>res(i.naturalWidth===960);i.onerror=()=>res(false);i.src='/public/media/jpeg-xl/image.jxl?conf='+Math.floor(performance.now());});}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/images.html"
    },
    {
      "id": "picture-negotiation-skips-undecodable-types",
      "description": "A <picture> whose first source declares an unsupported type must fall through to the next candidate. This is what makes shipping a new format safe, and it must keep working regardless of whether JPEG XL itself decodes.",
      "kind": "script",
      "test": "(async()=>{try{const p=document.createElement('picture');p.style.cssText='position:absolute;visibility:hidden';const s=document.createElement('source');s.srcset='/public/media/jpeg-xl/image.jxl';s.type='image/x-definitely-not-real';const img=document.createElement('img');img.src='/public/media/jpeg-xl/image.jpg';p.append(s,img);document.body.append(p);const ok=await new Promise((res)=>{img.onload=()=>res(img.currentSrc.endsWith('image.jpg'));img.onerror=()=>res(false);setTimeout(()=>res(img.currentSrc.endsWith('image.jpg')),3000);});p.remove();return ok;})()",
      "specSection": "https://html.spec.whatwg.org/multipage/images.html#update-the-source-set"
    },
    {
      "id": "jxl-is-decode-only",
      "description": "Chrome ships a JPEG XL decoder, not an encoder: canvas.toBlob with image/jxl must fall back to PNG rather than producing a JXL. Asserting the boundary keeps the feature honestly scoped.",
      "kind": "script",
      "test": "(async()=>{const c=document.createElement('canvas');c.width=8;c.height=8;const t=await new Promise((res)=>c.toBlob(res,'image/jxl'));return Boolean(t)&&t.type!=='image/jxl';})()",
      "specSection": "https://html.spec.whatwg.org/multipage/canvas.html#dom-canvas-toblob"
    }
  ]
}
