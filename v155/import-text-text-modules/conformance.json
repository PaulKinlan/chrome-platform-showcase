{
  "release": "v155",
  "featureSlug": "import-text-text-modules",
  "chromestatusId": 5191512456560640,
  "specUrl": "https://tc39.es/proposal-import-text/",
  "generatedAt": "2026-08-28T01:45:00Z",
  "author": "claude-opus-5-v155-build",
  "assertions": [
    {
      "id": "import-attributes-syntax-parses",
      "description": "The `with` options bag on dynamic import parses. Text modules are an import attribute, so this is the syntax they are built on.",
      "kind": "script",
      "test": "(()=>{try{new Function('return import(\"data:text/javascript,export default 1\", { with: {} })');return true;}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-import-attributes/"
    },
    {
      "id": "text-fixture-served-as-text-plain",
      "description": "The fixture is served with a text/* Content-Type. A text module is still a module fetch, so a mislabelled response would fail the import for a reason unrelated to the feature.",
      "kind": "script",
      "test": "(async()=>{try{const r=await fetch('/v155/import-text-text-modules/fixtures/greeting.txt',{method:'HEAD'});return (r.headers.get('content-type')||'').startsWith('text/plain');}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-import-text/"
    },
    {
      "id": "text-import-yields-a-string",
      "description": "Importing a file with type \"text\" must resolve to a module whose default export is a string containing the file's contents.",
      "kind": "script",
      "test": "(async()=>{try{const m=await import('/v155/import-text-text-modules/fixtures/greeting.txt',{with:{type:'text'}});return typeof m.default==='string'&&m.default.includes('text module');}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-import-text/"
    },
    {
      "id": "text-import-default-is-the-only-export",
      "description": "A text module exposes exactly one export, the default. Extra named exports would mean the loader is doing more than handing back a string.",
      "kind": "script",
      "test": "(async()=>{try{const m=await import('/v155/import-text-text-modules/fixtures/schema.sql',{with:{type:'text'}});const keys=Object.keys(m);return keys.length===1&&keys[0]==='default';}catch(e){return false;}})()",
      "specSection": "https://tc39.es/proposal-import-text/"
    },
    {
      "id": "text-import-is-cached-by-specifier",
      "description": "The module map caches by specifier, so importing the same URL twice returns the identical module object. This is what makes a text import a dependency rather than an operation.",
      "kind": "script",
      "test": "(async()=>{try{const url='/v155/import-text-text-modules/fixtures/shader.glsl';const a=await import(url,{with:{type:'text'}});const b=await import(url,{with:{type:'text'}});return a===b;}catch(e){return false;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/webappapis.html#module-map"
    },
    {
      "id": "missing-text-module-rejects",
      "description": "A missing file must reject the import. fetch() resolves for a 404 and leaves the check to the caller; an import cannot be ignored by accident, which is the practical difference between the two.",
      "kind": "script",
      "test": "(async()=>{try{await import('/v155/import-text-text-modules/fixtures/definitely-not-here.txt',{with:{type:'text'}});return false;}catch(e){return true;}})()",
      "specSection": "https://html.spec.whatwg.org/multipage/webappapis.html#fetch-a-single-module-script"
    }
  ]
}
