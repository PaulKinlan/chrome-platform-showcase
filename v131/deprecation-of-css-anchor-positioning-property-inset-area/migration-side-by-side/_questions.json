{
  "release": "v131",
  "featureSlug": "deprecation-of-css-anchor-positioning-property-inset-area",
  "conceptSlug": "migration-side-by-side",
  "chromestatusId": 6567965055778816,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "True A/B: deprecated `inset-area` on the left panel, new `position-area` on the right, shared direction value. Demonstrates the rename's behaviour-preserving promise."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Direction <select> drives both panels; real comparison."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Card promises 'A/B with same anchor, popover, direction values' — page delivers."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "The hardcoded .pop-new uses `position-area: top` but the JS only updates inline style; need to verify the JS actually flips position-area too. Looking at the snippet, .pop-new has a hardcoded `position-area: top` declaration which would be overridden by the inline-style update."
    },
    "references": {
      "state": "fail",
      "rationale": "See also links to chromestatus.com/feature/5239846558531584 — WRONG feature ID. This belongs to feature 6567965055778816. Critical rule #3 regression."
    },
    "design_system": {
      "state": "pass",
      "rationale": "CSS variables; .tag-dep / .tag-new both set background and color together."
    }
  },
  "openQuestions": [
    {
      "title": "Fix the wrong chromestatus link",
      "detail": "See also points at chromestatus.com/feature/5239846558531584 but this concept belongs to feature 6567965055778816. Slug-recovery script would relocate this folder.",
      "severity": "major"
    },
    {
      "title": "Verify both panels actually update on direction change",
      "detail": "The CSS has `.pop-new { position-area: top; }` hardcoded. If the JS only writes inline-style updates, that's fine — but if it doesn't update .pop-new, the right panel will stay at 'top' while the left panel moves. Spot-check that the JS sets both panels' position-area / inset-area properly.",
      "severity": "moderate"
    }
  ],
  "summary": "Clean A/B layout that hits the spec's behavioural-equivalence promise. Ships the WRONG chromestatus feature ID — major rule #3 regression — and the JS direction update needs a spot-check to ensure both panels stay in sync."
}
