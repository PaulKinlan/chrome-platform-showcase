{
  "release": "v131",
  "featureSlug": "css-anchor-positioning-allow-anchor-size-in-inset-and-margin-properties",
  "conceptSlug": "responsive-tooltip",
  "chromestatusId": 5203950077476864,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Tooltip's top inset is `calc(anchor(bottom) + anchor-size(--target height) * 0.25)` and margin-right is `calc(anchor-size(--target width) * 0.1)` — anchor-size() in BOTH inset and margin, the exact spec change."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Slider rescales the target font-size; tooltip offset rescales declaratively via CSS without JS measurement."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Feature card promises a slider-driven scaling tooltip offset; that is the deliverable."
    },
    "edge_cases": {
      "state": "fail",
      "rationale": "No fallback for pre-131 browsers, no readout of computed values (visitor cannot verify the offset really is 25% of height), and the tooltip overlaps a fixed-height stage so at large font sizes the tooltip can overflow the container — never visually flagged."
    },
    "references": {
      "state": "fail",
      "rationale": "See also has NO chromestatus.com link and NO spec link — only sibling concept links. Violates CLAUDE.md rule #3 (id embed self-heal)."
    },
    "design_system": {
      "state": "pass",
      "rationale": "CSS variables used throughout, --accent-blue/--bg-ivory pairing for tooltip passes WCAG AA, --text-black/--bg-ivory for target also pass."
    }
  },
  "openQuestions": [
    {
      "title": "Add the chromestatus.com reference link",
      "detail": "CLAUDE.md rule #3: every page needs https://chromestatus.com/feature/5203950077476864 in see-also. Currently sibling-only links break the slug recovery script.",
      "severity": "major"
    },
    {
      "title": "Surface the computed offset live",
      "detail": "Right now the visitor sees the tooltip move but cannot verify the maths. Add a `<output>` showing `getComputedStyle(tooltip).top` in pixels, updated on each input event. Makes the spec change concrete instead of theoretical.",
      "severity": "minor"
    },
    {
      "title": "Show what happens with no font-size transition",
      "detail": "The lede notes 'before v131 the JS resize event broke under transitions'. Add a toggle: 'animate target' vs 'snap target' to highlight the difference. With the CSS approach, both work; with JS, only the snap version does.",
      "severity": "minor"
    }
  ],
  "summary": "Hits the spec change accurately and the live demo is convincing, but misses the chromestatus reference link (a major rule #3 regression) and never surfaces the computed offset for visitors to verify."
}
