{
  "release": "v151",
  "featureSlug": "chrome-will-remove-support-for-macos-12",
  "chromestatusId": 5077742779498496,
  "specUrl": "https://chromestatus.com/feature/5077742779498496",
  "generatedAt": "2026-06-01T08:13:45Z",
  "author": "conformance-author-v1-subagent",
  "assertions": [
    {
      "id": "user-agent-string-exposed",
      "description": "navigator.userAgent is exposed so the platform string can be read; this is the surface Chrome used to communicate the OS deprecation infobar trigger.",
      "kind": "typeof",
      "test": "navigator.userAgent",
      "expect": "string",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "user-agent-data-platform-exposed",
      "description": "navigator.userAgentData.platform exposes the OS name used by deprecation-targeted features. Chrome exposes this; other browsers may not.",
      "kind": "script",
      "test": "(()=>{const d=navigator.userAgentData;return !!d && typeof d.platform==='string';})()",
      "expectedBrowsers": ["chrome"]
    },
    {
      "id": "high-entropy-platform-version-available",
      "description": "navigator.userAgentData.getHighEntropyValues() resolves with a platformVersion string — the field Chrome reads to detect macOS 12 builds that fall outside the support window.",
      "kind": "script",
      "test": "(()=>{const d=navigator.userAgentData;return !!d && typeof d.getHighEntropyValues==='function';})()",
      "expectedBrowsers": ["chrome"]
    },
    {
      "id": "platform-string-not-empty",
      "description": "navigator.platform is non-empty so legacy macOS detection paths keep working for the warning infobar period.",
      "kind": "script",
      "test": "typeof navigator.platform==='string' && navigator.platform.length>0",
      "expectedBrowsers": ["chrome", "firefox", "safari"]
    },
    {
      "id": "chrome-version-token-present",
      "description": "The user-agent contains a Chrome/<version> token so server-side feature gates can detect Chrome 151+ rejections of macOS 12 hosts.",
      "kind": "script",
      "test": "/Chrome\\/(\\d+)/.test(navigator.userAgent)",
      "expectedBrowsers": ["chrome"]
    }
  ]
}
