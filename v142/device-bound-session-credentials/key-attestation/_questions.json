{
  "release": "v142",
  "featureSlug": "device-bound-session-credentials",
  "conceptSlug": "key-attestation",
  "chromestatusId": 5140168270413824,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "pass",
      "rationale": "Compares the four DBSC key-storage tiers (software, TEE, TPM 2.0, Secure Enclave / Strongbox) across attack classes, which is exactly the threat model the webappsec-dbsc spec uses to motivate hardware binding."
    },
    "interactive": {
      "state": "pass",
      "rationale": "Four tier buttons drive a live score matrix against four attack classes, with scores updating per selection and concrete-attack rationale text appearing for each."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Feature blurb sells 'compare software-only fallback, TEE, TPM 2.0, Secure Enclave across four attack classes' — page implements exactly that with a tier × attack matrix."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Tiers and attack classes are covered. Missing: the 'no key store at all' degraded mode (when policy denies storage), and the cross-keystore migration story (key rotation between tiers when a user changes devices)."
    },
    "references": {
      "state": "fail",
      "rationale": "The see-also list links the webappsec-dbsc spec but does NOT include https://chromestatus.com/feature/5140168270413824. Same design-system rule 3 violation as the sibling refresh-loop page; both this and refresh-loop omit the canonical chromestatus link."
    },
    "design_system": {
      "state": "pass",
      "rationale": "Tier-selector buttons override background+color together; matrix cells use --accent-emerald/amber/rose; no static-card pattern."
    }
  },
  "openQuestions": [
    {
      "title": "CRITICAL: add the missing chromestatus.com/feature/<id> link",
      "detail": "Same defect as refresh-loop: the chromestatus.com/feature/5140168270413824 link is missing from this page's see-also list. Design system rule 3 requires it, and the fix-slugs.py recovery script depends on it. Add the link.",
      "severity": "major"
    },
    {
      "title": "Add a key-rotation / device-migration scenario",
      "detail": "When a user gets a new device, DBSC has to re-bind the session to the new keystore. The matrix today is static (one tier at a time); a 'migrate from tier X to tier Y' button would surface the cross-tier handshake which is one of the operational pain points the spec acknowledges.",
      "suggestedSlug": "key-store-migration",
      "severity": "moderate"
    },
    {
      "title": "Cite the attack-class sources",
      "detail": "The 'concrete attacks each tier defeats' text reads authoritatively but doesn't link to threat-model sources (e.g. SOK papers on TPM attacks, public Strongbox / Secure Enclave docs). Add citation links under each attack-class column header so authors can dig in.",
      "severity": "minor"
    }
  ],
  "summary": "Strong threat-model explainer with a real matrix UI, but it shares the refresh-loop defect: the chromestatus link is missing, violating CLAUDE.md rule 3. Fix that, then add a tier-migration scenario for the device-swap edge case."
}
