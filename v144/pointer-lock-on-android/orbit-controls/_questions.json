{
  "release": "v144",
  "featureSlug": "pointer-lock-on-android",
  "conceptSlug": "orbit-controls",
  "chromestatusId": 6739764319485952,
  "reviewedAt": "2026-06-01T00:00:00Z",
  "reviewer": "reviewer-v1-subagent",
  "rubric": {
    "spec_match": {
      "state": "partial",
      "rationale": "Chromium-catches-up feature — the spec already documents the behaviour, so the value of the demo is the interop story. The concept exercises one corner but the before/after comparison isn't always crisp."
    },
    "interactive": {
      "state": "fail",
      "rationale": "No real form controls driving the script — page is closer to a styled code card than an interactive demo."
    },
    "blurb_alignment": {
      "state": "pass",
      "rationale": "Concept page lede restates the feature-index blurb's key promise (overlap on android, arena, cube, drag)."
    },
    "edge_cases": {
      "state": "partial",
      "rationale": "Demo runs the happy path; no visible feature detection, error handling, or no-support fallback in the script."
    },
    "references": {
      "state": "fail",
      "rationale": "No chromestatus.com/feature/<id> link on the concept page — CLAUDE.md rule #3 says every page must carry the self-heal reference, not rely on the parent feature index."
    },
    "design_system": {
      "state": "pass",
      "rationale": "All colours come from the shared CSS variables and the layout follows the stage + controls editorial pattern."
    }
  },
  "openQuestions": [
    {
      "title": "Cover the spec aspect: no-pointer device fallback (touch input)",
      "detail": "The pointer-lock-on-android feature's headline framing — Pointer Lock API now available on Android (raw mouse deltas, cursor hidden) — calls out \"no-pointer device fallback (touch input)\". This concept page does not exercise that, so the spec story is incomplete from this page alone. Either extend this demo or land a sibling concept dedicated to it.",
      "severity": "moderate"
    },
    {
      "title": "Cover the spec aspect: first-person camera / orbit controls as the canonical use ca",
      "detail": "The pointer-lock-on-android feature's headline framing — Pointer Lock API now available on Android (raw mouse deltas, cursor hidden) — calls out \"first-person camera / orbit controls as the canonical use cases\". This concept page does not exercise that, so the spec story is incomplete from this page alone. Either extend this demo or land a sibling concept dedicated to it.",
      "severity": "moderate"
    },
    {
      "title": "Add real controls so the demo isn't a styled code card",
      "detail": "Concept pages are supposed to be hands-on. With only 0 button(s), 0 input(s), 0 select(s), 0 range slider(s), the surface is thin. Add at least one slider, picker, or interactive readout so visitors can actually probe the API from the page.",
      "severity": "major"
    },
    {
      "title": "Add the chromestatus.com/feature link to this page",
      "detail": "Per CLAUDE.md rule #3, every concept page must include the https://chromestatus.com/feature/6739764319485952 link directly — it is the self-heal anchor used by the slug-recovery script. Right now the page links only back to the parent feature index.",
      "severity": "moderate"
    },
    {
      "title": "Cover at least one realistic failure mode",
      "detail": "Add feature detection at the top of the script and surface the result. Handle the permission-denied / API-rejection case in the catch branch with a visible message. If the feature is flag-gated or origin-trial, show explicitly what the page does when the API is absent.",
      "severity": "minor"
    }
  ],
  "summary": "orbit-controls under pointer-lock-on-android: 2 pass / 2 partial / 2 fail. Interactive surface is thin, design system clean, spec story is partial — see open questions for the angles still missing."
}
